# 截图工具快速使用指南

## 🚀 快速开始

### 1. 安装依赖
```bash
npm install playwright
npx playwright install chromium
```

### 2. 生成截图
```bash
# 生成单个页面截图
node generate_screenshot.js home

# 查看所有可用页面
node generate_screenshot.js
```

### 3. 批量生成
```bash
# 创建批处理脚本
cat > batch_screenshot.sh << 'EOF'
#!/bin/bash
pages=("home" "login" "profile" "splash" "settings")
for page in "${pages[@]}"; do
    echo "正在生成 $page 页面截图..."
    node generate_screenshot.js $page
done
echo "所有截图生成完成！"
EOF

chmod +x batch_screenshot.sh
./batch_screenshot.sh
```

## 📱 输出结果

截图将保存在 `html/png/` 目录下：
- 尺寸：393×852px (iPhone 12 Pro)
- 分辨率：2x (高清)
- 格式：PNG

## 🛠️ 常用命令

```bash
# 生成主要页面
node generate_screenshot.js home
node generate_screenshot.js login
node generate_screenshot.js profile
node generate_screenshot.js splash

# 生成功能页面
node generate_screenshot.js settings
node generate_screenshot.js chat
node generate_screenshot.js moments

# 生成活动页面
node generate_screenshot.js buddy-matching
node generate_screenshot.js offline-activity-list
node generate_screenshot.js publish-buddy-activity
```

## ❓ 常见问题

**Q: 截图模糊？**
A: 确保使用2x设备像素比，脚本已自动设置。

**Q: 页面加载不完整？**
A: 脚本会等待网络空闲，如有问题可增加等待时间。

**Q: 找不到页面？**
A: 确保HTML文件在 `html/` 目录下，文件名与参数一致。

## 📋 可用页面列表

- `home` - 首页
- `login` - 登录页
- `profile` - 个人资料
- `splash` - 启动页
- `settings` - 设置
- `chat` - 聊天
- `moments` - 动态
- `buddy-matching` - 伙伴匹配
- `offline-activity-list` - 线下活动列表
- `publish-buddy-activity` - 发布伙伴活动
- 更多页面请运行 `node generate_screenshot.js` 查看

---

💡 **提示**: 首次使用需要安装 Playwright 浏览器，可能需要几分钟时间。
