# 碰碰交友社交应用 - Java后端系统分析文档

> **项目基础**: wxmini-friends
> **技术栈**: Spring Boot 2.7.18 + MyBatis-Plus + MySQL + Redis + JWT + 微信支付
> **文档版本**: v2.0（完整业务逻辑流程图）  
> **编写日期**: 2025-01-01  
> **最后更新**: 2025-11-07
> **接口总数**: 50个
> **数据表总数**: 17张

---

## 📋 目录

1. [系统概述](#1-系统概述)
2. [现有基础分析](#2-现有基础分析)
3. [核心模块设计](#3-核心模块设计)
4. [数据库设计](#4-数据库设计)
5. [接口设计](#5-接口设计)
6. [会员功能开关设计](#6-会员功能开关设计)
7. [关键业务流程与逻辑图](#7-关键业务流程与逻辑图)
   - [7.1 用户注册与资料完善流程](#71-用户注册与资料完善流程)
   - [7.2 智能匹配与互动流程](#72-智能匹配与互动流程)
   - [7.3 动态广场发布流程](#73-动态广场发布流程)
   - [7.4 活动报名与管理流程](#74-活动报名与管理流程)
   - [7.5 VIP支付流程](#75-vip支付流程)
   - [7.6 实名认证流程](#76-实名认证流程)
   - [7.7 图片管理流程（两步上传）](#77-图片管理流程两步上传)
   - [7.8 原有业务流程说明](#78-原有业务流程说明)
8. [数据字典](#8-数据字典)
9. [性能优化方案](#9-性能优化方案)
10. [安全设计](#10-安全设计)
11. [部署方案](#11-部署方案)
12. [监控与运维](#12-监控与运维)
13. [附录](#13-附录)
14. [总结](#14-总结)
15. [技术实现细节补充](#15-技术实现细节补充)
    - [15.1 动态广场模块技术实现](#151-动态广场模块技术实现)
    - [15.2 微信支付技术实现](#152-微信支付技术实现)

---

## 1. 系统概述

### 1.1 产品定位
- **产品名称**: 碰碰（微信小程序）
- **核心功能**: 恋爱交友 + 搭子平台
- **目标用户**: 18-35岁单身用户
- **核心理念**: 做一个懂你的朋友

### 1.2 技术架构
```
微信小程序前端
    ├── 业务逻辑（匹配、动态、搭子）
    └── 聊天功能（腾讯云IM SDK）
         ↓ HTTPS
Spring Boot 应用层
    ├── Controller (接口层)
    ├── Service (业务层)
    ├── Mapper (数据层)
    └── Utils (工具层)
    ↓
    ├── MySQL 8.0 + Redis（业务数据）
    └── 腾讯云IM（聊天消息）
```

### 1.3 已有依赖
- Spring Boot Starter Web
- MyBatis-Plus 3.5.3.1
- MySQL Connector 8.0.33
- Redis
- JWT (jjwt 0.11.5)
- Hutool 5.8.20
- FastJson 1.2.83
- Swagger 3.0

### 1.4 新增依赖（聊天模块）
- **腾讯云IM服务端SDK** (tencentcloud-sdk-java-im 3.1.800)
  - 用于生成UserSig、同步用户资料、处理IM回调
  - 前期免费（100万DAU以下）
- **微信小程序端SDK** (tim-wx-sdk)
  - 前端集成，处理即时通讯

---

## 2. 现有基础分析

### 2.1 已实现功能

#### 2.1.1 用户认证模块
**WxAuthController** - 微信登录认证
```java
现有接口：
- POST /api/auth/wx/login        // 微信登录
- GET  /api/auth/user/info       // 获取用户信息
- POST /api/auth/token/refresh   // 刷新Token

实现状态：✅ 已完成
数据表：t_user, t_wx_user
```

#### 2.1.2 用户资料模块
**UserProfileController** - 用户资料管理
```java
现有接口：
- POST /api/profile/complete     // 完善资料
- PUT  /api/profile/update       // 更新资料
- GET  /api/profile/{userId}     // 查看资料
- POST /api/profile/tags         // 保存标签
- GET  /api/tags/library         // 获取标签库
- POST /api/profile/photos       // 上传照片

实现状态：✅ 已完成
数据表：t_user_profile, t_user_tags, t_user_album, t_tag_library
```

#### 2.1.3 基础配置
- JWT认证拦截器：已配置
- 统一异常处理：已配置
- Redis缓存：已配置
- Swagger文档：已配置
- 日志追踪（TraceId）：已配置

### 2.2 待开发功能

#### 核心功能模块
1. ❌ 智能匹配系统（核心）
2. ❌ 聊天通讯模块
3. ❌ 动态广场模块
4. ❌ 搭子平台模块
5. ❌ 线下活动模块
6. ❌ 会员与支付模块
7. ❌ 实名认证模块（不含学历认证）

---

## 3. 核心模块设计

### 3.1 智能匹配系统

#### 3.1.1 模块概述
**核心价值**: 应用的最核心功能，通过多维度算法为用户推荐合适的对象

**业务流程**:
```
用户进入主页 → 获取推荐列表 → 查看用户卡片 → 
执行操作（喜欢/跳过/超级喜欢）→ 匹配判定 → 
成功则可以无限制聊天（由腾讯云IM处理）
```

#### 3.1.2 技术实现设计

**Controller层**: `MatchingController`
```java
@RestController
@RequestMapping("/api/matching")
public class MatchingController {
    
    // 获取推荐用户列表
    @GetMapping("/recommend")
    public Result<Page<UserCardDTO>> recommendUsers(
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
    
    // 喜欢操作
    @PostMapping("/like")
    public Result<MatchResultDTO> likeUser(
        @RequestBody @Validated LikeRequest request,
        HttpServletRequest httpRequest
    );
    
    // 超级喜欢操作
    @PostMapping("/super-like")
    public Result<MatchResultDTO> superLikeUser(
        @RequestBody @Validated SuperLikeRequest request,
        HttpServletRequest httpRequest
    );
    
    // 跳过操作
    @PostMapping("/pass")
    public Result<Void> passUser(
        @RequestBody @Validated PassRequest request,
        HttpServletRequest httpRequest
    );
    
    // 保存/更新匹配偏好
    @PostMapping("/preference")
    public Result<Void> savePreference(
        @RequestBody @Validated MatchingPreferenceRequest request,
        HttpServletRequest httpRequest
    );
    
    // 获取匹配偏好
    @GetMapping("/preference")
    public Result<MatchingPreferenceDTO> getPreference(
        HttpServletRequest request
    );
    
    // 查看谁喜欢我
    @GetMapping("/liked-me")
    public Result<Page<UserCardDTO>> getLikedMe(
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
}
```

**Service层**: `MatchingService`
```java
@Service
public interface MatchingService {
    
    /**
     * 获取推荐用户列表
     * @param userId 当前用户ID
     * @param page 页码
     * @param size 每页数量
     * @return 推荐用户列表
     */
    Page<UserCardDTO> recommendUsers(Long userId, int page, int size);
    
    /**
     * 喜欢操作
     * @param fromUserId 发起用户ID
     * @param toUserId 目标用户ID
     * @param matchType 匹配类型：1-普通喜欢，2-超级喜欢
     * @return 匹配结果
     */
    MatchResultDTO likeUser(Long fromUserId, Long toUserId, int matchType);
    
    /**
     * 跳过操作
     * @param fromUserId 发起用户ID
     * @param toUserId 目标用户ID
     */
    void passUser(Long fromUserId, Long toUserId);
    
    /**
     * 检查匹配限制
     * @param userId 用户ID
     * @param matchType 匹配类型
     * @return 是否可以继续匹配
     */
    boolean checkMatchLimit(Long userId, int matchType);
    
    /**
     * 计算匹配分数
     * @param currentUserId 当前用户ID
     * @param targetUserId 目标用户ID
     * @return 匹配分数（0-100）
     */
    double calculateMatchScore(Long currentUserId, Long targetUserId);
}
```

#### 3.1.3 匹配算法详细设计

**算法权重配置**（可通过配置表动态调整）:
```java
地理位置匹配：30%
兴趣标签相似度：25%
年龄匹配度：20%
基本信息匹配：15%（身高、职业、婚姻状况等）
活跃度：10%
```

**核心算法实现思路**:
```java
public double calculateMatchScore(Long userId1, Long userId2) {
    double score = 0.0;
    
    // 1. 地理位置得分（30%）
    double locationScore = calculateLocationScore(userId1, userId2);
    score += locationScore * getWeight("location");  // 0.30
    
    // 2. 兴趣标签相似度（25%）
    double tagScore = calculateTagSimilarity(userId1, userId2);
    score += tagScore * getWeight("tag");  // 0.25
    
    // 3. 年龄匹配度（20%）
    double ageScore = calculateAgeScore(userId1, userId2);
    score += ageScore * getWeight("age");  // 0.20
    
    // 4. 基本信息匹配（15%）
    double basicScore = calculateBasicInfoScore(userId1, userId2);
    score += basicScore * getWeight("basic");  // 0.15
    
    // 5. 活跃度得分（10%）
    double activityScore = calculateActivityScore(userId2);
    score += activityScore * getWeight("activity");  // 0.10
    
    return score;
}

// 地理位置得分计算
private double calculateLocationScore(Long userId1, Long userId2) {
    // 使用Haversine公式计算距离
    // 距离越近分数越高
    // 返回0-100分
}

// 兴趣标签相似度计算（Jaccard相似度）
private double calculateTagSimilarity(Long userId1, Long userId2) {
    Set<String> tags1 = getUserTags(userId1);
    Set<String> tags2 = getUserTags(userId2);
    
    // 交集
    Set<String> intersection = new HashSet<>(tags1);
    intersection.retainAll(tags2);
    
    // 并集
    Set<String> union = new HashSet<>(tags1);
    union.addAll(tags2);
    
    if (union.isEmpty()) return 0;
    
    double similarity = (double) intersection.size() / union.size();
    return similarity * 100;
}
```

#### 3.1.4 匹配限制逻辑

**会员功能开关控制**:
```java
@Service
public class MatchLimitService {
    
    public boolean checkMatchLimit(Long userId, int matchType) {
        // 1. 读取会员功能开关
        boolean vipEnabled = systemConfigService.getBoolean(
            "vip.feature.enabled", false
        );
        
        // 2. 如果会员功能关闭，全功能免费
        if (!vipEnabled) {
            return true;  // 不限制
        }
        
        // 3. 会员功能开启，检查VIP状态
        VipMember vip = vipService.getValidVip(userId);
        if (vip != null) {
            return true;  // VIP用户不限制
        }
        
        // 4. 非VIP用户检查每日限制
        MatchLimit limit = getOrCreateTodayLimit(userId);
        
        if (matchType == 1) {
            // 普通喜欢
            int freeLimit = systemConfigService.getInt(
                "vip.daily.free.match.count", 3
            );
            return limit.getFreeMatchCount() < freeLimit;
        } else if (matchType == 2) {
            // 超级喜欢
            int superLikeLimit = systemConfigService.getInt(
                "vip.daily.free.super.like.count", 1
            );
            return limit.getSuperLikeCount() < superLikeLimit;
        }
        
        return false;
    }
    
    // 获取或创建今日限制记录
    private MatchLimit getOrCreateTodayLimit(Long userId) {
        LocalDate today = LocalDate.now();
        MatchLimit limit = matchLimitMapper.selectOne(
            new LambdaQueryWrapper<MatchLimit>()
                .eq(MatchLimit::getUserId, userId)
                .eq(MatchLimit::getDate, today)
        );
        
        if (limit == null) {
            limit = new MatchLimit();
            limit.setUserId(userId);
            limit.setDate(today);
            limit.setFreeMatchCount(0);
            limit.setSuperLikeCount(0);
            matchLimitMapper.insert(limit);
        }
        
        return limit;
    }
}
```

---

### 3.2 聊天通讯模块

#### 3.2.1 模块概述
**核心功能**: 用户之间的即时通讯，支持文字和图片消息

**防骚扰机制**: 
- 未匹配用户：只能发送1条消息，对方回复后解除限制
- 已匹配用户：无限制正常聊天

#### 3.2.2 技术方案选型：第三方IM服务（推荐）

**💡 为什么使用第三方IM服务？**

使用第三方IM服务相比自建聊天系统有以下优势：

| 对比项 | 自建系统 | 第三方IM |
|-------|---------|----------|
| **开发成本** | 需开发WebSocket、消息存储、推送系统 | SDK集成，2-3天完成 |
| **稳定性** | 需处理各种异常场景 | 经过亿级用户验证 |
| **功能** | 基础聊天，扩展困难 | 消息撤回、多媒体、群聊等开箱即用 |
| **运维** | 需专人维护服务器、监控、扩容 | 官方运维，按量付费 |
| **多端同步** | 需自行实现 | 原生支持多端消息同步 |
| **离线推送** | 需对接推送服务 | 内置离线推送能力 |

**✅ 推荐方案：腾讯云IM（Tencent Cloud IM）**

选择理由：
1. **小程序友好**：官方提供微信小程序SDK，集成简单
2. **功能丰富**：支持单聊、群聊、离线推送、消息撤回、已读回执
3. **性价比高**：提供免费额度（100万DAU以下），按量付费
4. **文档完善**：中文文档齐全，示例代码丰富
5. **稳定可靠**：QQ、微信同款底层技术

**备选方案**：
- 融云（Rongyun）：性能优秀，但价格偏高
- 环信（Easemob）：功能全面，适合大型企业

#### 3.2.3 后端集成方案

**依赖添加** (`pom.xml`)：
```xml
<!-- 腾讯云IM服务端SDK -->
<dependency>
    <groupId>com.tencentcloudapi</groupId>
    <artifactId>tencentcloud-sdk-java-im</artifactId>
    <version>3.1.800</version>
</dependency>
```

**配置文件** (`application.yml`)：
```yaml
tencent:
  im:
    sdk-app-id: 你的SDKAPPID
    secret-key: 你的密钥
    admin-user-id: administrator  # 管理员UserID
```

**配置类**：
```java
@Configuration
@ConfigurationProperties(prefix = "tencent.im")
@Data
public class TencentImConfig {
    private Long sdkAppId;
    private String secretKey;
    private String adminUserId;
}
```

#### 3.2.4 核心功能实现

**Controller层**: `ChatController`
```java
@RestController
@RequestMapping("/api/chat")
@Api(tags = "聊天模块")
public class ChatController {
    
    @Autowired
    private TencentImService tencentImService;
    
    @Autowired
    private ChatPermissionService chatPermissionService;
    
    /**
     * 生成IM用户签名（UserSig）
     * 小程序端需要UserSig才能登录IM
     */
    @GetMapping("/user-sig")
    @ApiOperation("获取IM登录凭证")
    public Result<UserSigDTO> getUserSig(HttpServletRequest request) {
        Long userId = JwtUtils.getUserIdFromRequest(request);
        String userSig = tencentImService.generateUserSig(userId.toString());
        
        return Result.success(UserSigDTO.builder()
            .userId(userId.toString())
            .userSig(userSig)
            .sdkAppId(tencentImService.getSdkAppId())
            .build());
    }
    
    /**
     * 检查发送权限（防骚扰）
     * 在小程序发送消息前先调用此接口
     */
    @PostMapping("/check-permission")
    @ApiOperation("检查发送权限")
    public Result<ChatPermissionDTO> checkSendPermission(
        @RequestBody @Validated CheckPermissionRequest request,
        HttpServletRequest httpRequest
    ) {
        Long senderId = JwtUtils.getUserIdFromRequest(httpRequest);
        Long receiverId = request.getReceiverId();
        
        ChatPermissionDTO permission = chatPermissionService
            .checkPermission(senderId, receiverId);
        
        return Result.success(permission);
    }
    
    /**
     * 消息发送回调（由腾讯云IM回调）
     * 用于记录消息、更新防骚扰计数
     */
    @PostMapping("/callback/after-send-msg")
    @ApiOperation("消息发送后回调")
    public CallbackResponse afterSendMsg(
        @RequestBody TencentImCallback callback
    ) {
        // 解析消息
        Long senderId = Long.parseLong(callback.getFromAccount());
        Long receiverId = Long.parseLong(callback.getToAccount());
        
        // 更新防骚扰记录
        chatPermissionService.recordMessage(senderId, receiverId);
        
        // 返回成功，让消息正常发送
        return CallbackResponse.success();
    }
    
    /**
     * 同步用户资料到IM系统
     */
    @PostMapping("/sync-profile")
    @ApiOperation("同步用户资料")
    public Result<Void> syncUserProfile(HttpServletRequest request) {
        Long userId = JwtUtils.getUserIdFromRequest(request);
        tencentImService.syncUserProfile(userId);
        return Result.success();
    }
}
```

**Service层**: `TencentImService`
```java
@Service
@Slf4j
public class TencentImService {
    
    @Autowired
    private TencentImConfig config;
    
    @Autowired
    private UserProfileMapper userProfileMapper;
    
    /**
     * 生成UserSig（用户登录IM的凭证）
     */
    public String generateUserSig(String userId) {
        try {
            TLSSigAPIv2 api = new TLSSigAPIv2(
                config.getSdkAppId(), 
                config.getSecretKey()
            );
            // 有效期7天
            return api.genUserSig(userId, 7 * 24 * 3600);
        } catch (Exception e) {
            log.error("生成UserSig失败", e);
            throw new BusinessException("生成IM登录凭证失败");
        }
    }
    
    /**
     * 同步用户资料到IM系统
     * 在用户首次登录或修改资料后调用
     */
    public void syncUserProfile(Long userId) {
        UserProfile profile = userProfileMapper.selectById(userId);
        
        try {
            // 构建IM用户资料
            PortraitSetRequest request = new PortraitSetRequest();
            request.setFromAccount(userId.toString());
            
            ProfileItem[] items = new ProfileItem[]{
                new ProfileItem("Tag_Profile_IM_Nick", profile.getNickname()),
                new ProfileItem("Tag_Profile_IM_Image", profile.getAvatarUrl()),
                new ProfileItem("Tag_Profile_IM_Gender", 
                    profile.getGender() == 1 ? "Gender_Type_Male" : "Gender_Type_Female")
            };
            request.setProfileItem(items);
            
            // 调用API同步
            ImClient.getImService().portraitSet(request);
            
        } catch (Exception e) {
            log.error("同步用户资料失败", e);
            // 不抛异常，避免影响主流程
        }
    }
    
    public Long getSdkAppId() {
        return config.getSdkAppId();
    }
}
```

#### 3.2.5 防骚扰逻辑设计

**Service层**: `ChatPermissionService`
```java
@Service
@Slf4j
public class ChatPermissionService {
    
    @Autowired
    private ChatLimitMapper chatLimitMapper;
    
    @Autowired
    private MatchRecordMapper matchRecordMapper;
    
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    
    /**
     * 检查发送权限
     */
    public ChatPermissionDTO checkPermission(Long senderId, Long receiverId) {
        // 1. 检查是否已匹配
        boolean isMatched = checkIfMatched(senderId, receiverId);
        
        if (isMatched) {
            return ChatPermissionDTO.builder()
                .canSend(true)
                .unlimited(true)
                .reason("已匹配用户，无限制聊天")
                .build();
        }
        
        // 2. 未匹配用户，检查防骚扰限制
        String cacheKey = String.format("chat:limit:%d:%d", senderId, receiverId);
        Integer sentCount = (Integer) redisTemplate.opsForValue().get(cacheKey);
        
        if (sentCount == null) {
            // 从数据库查询
            ChatLimit limit = chatLimitMapper.selectOne(
                new LambdaQueryWrapper<ChatLimit>()
                    .eq(ChatLimit::getSenderId, senderId)
                    .eq(ChatLimit::getReceiverId, receiverId)
            );
            sentCount = (limit != null) ? limit.getUnrepliedCount() : 0;
        }
        
        boolean canSend = sentCount < 1;  // 未匹配最多发1条
        
        return ChatPermissionDTO.builder()
            .canSend(canSend)
            .unlimited(false)
            .remainingCount(canSend ? (1 - sentCount) : 0)
            .reason(canSend ? "可以发送" : "对方未回复，暂时无法继续发送")
            .build();
    }
    
    /**
     * 记录消息发送（由IM回调触发）
     */
    public void recordMessage(Long senderId, Long receiverId) {
        // 1. 检查是否已匹配
        if (checkIfMatched(senderId, receiverId)) {
            return;  // 已匹配用户无需记录
        }
        
        // 2. 更新限制记录
        String cacheKey = String.format("chat:limit:%d:%d", senderId, receiverId);
        redisTemplate.opsForValue().increment(cacheKey);
        redisTemplate.expire(cacheKey, 7, TimeUnit.DAYS);
        
        // 3. 异步更新数据库
        CompletableFuture.runAsync(() -> {
            ChatLimit limit = chatLimitMapper.selectOne(
                new LambdaQueryWrapper<ChatLimit>()
                    .eq(ChatLimit::getSenderId, senderId)
                    .eq(ChatLimit::getReceiverId, receiverId)
            );
            
            if (limit == null) {
                limit = new ChatLimit();
                limit.setSenderId(senderId);
                limit.setReceiverId(receiverId);
                limit.setUnrepliedCount(1);
                chatLimitMapper.insert(limit);
            } else {
                limit.setUnrepliedCount(limit.getUnrepliedCount() + 1);
                chatLimitMapper.updateById(limit);
            }
        });
    }
    
    /**
     * 清除限制（对方回复后）
     */
    public void clearLimit(Long senderId, Long receiverId) {
        String cacheKey = String.format("chat:limit:%d:%d", receiverId, senderId);
        redisTemplate.delete(cacheKey);
        
        // 异步清除数据库记录
        CompletableFuture.runAsync(() -> {
            chatLimitMapper.delete(
                new LambdaQueryWrapper<ChatLimit>()
                    .eq(ChatLimit::getSenderId, receiverId)
                    .eq(ChatLimit::getReceiverId, senderId)
            );
        });
    }
    
    private boolean checkIfMatched(Long user1Id, Long user2Id) {
        Integer count = matchRecordMapper.selectCount(
            new LambdaQueryWrapper<MatchRecord>()
                .eq(MatchRecord::getMatchStatus, 2)  // 已匹配
                .and(w -> w
                    .and(w1 -> w1
                        .eq(MatchRecord::getUserId, user1Id)
                        .eq(MatchRecord::getTargetUserId, user2Id)
                    )
                    .or(w2 -> w2
                        .eq(MatchRecord::getUserId, user2Id)
                        .eq(MatchRecord::getTargetUserId, user1Id)
                    )
                )
        );
        return count > 0;
    }
}
```

#### 3.2.6 小程序端集成示例

**安装IM SDK**：
```bash
npm install tim-wx-sdk --save
```

**初始化IM**：
```javascript
// app.js
import TIM from 'tim-wx-sdk';

App({
  onLaunch() {
    // 初始化腾讯云IM
    this.tim = TIM.create({
      SDKAppID: this.globalData.sdkAppId
    });
    
    // 设置日志级别
    this.tim.setLogLevel(0); // 0: 普通，1: release，2: warn，3: error
  },
  
  async loginIM(userId, userSig) {
    try {
      await this.tim.login({
        userID: userId.toString(),
        userSig: userSig
      });
      console.log('IM登录成功');
    } catch (error) {
      console.error('IM登录失败', error);
    }
  }
})
```

**发送消息前检查权限**：
```javascript
// pages/chat/chat.js
async sendMessage(content) {
  // 1. 先调用后端检查权限
  const permissionRes = await wx.request({
    url: `${baseUrl}/api/chat/check-permission`,
    method: 'POST',
    data: {
      receiverId: this.data.receiverId
    },
    header: {
      'Authorization': `Bearer ${token}`
    }
  });
  
  if (!permissionRes.data.data.canSend) {
    wx.showToast({
      title: permissionRes.data.data.reason,
      icon: 'none'
    });
    return;
  }
  
  // 2. 权限通过，使用IM SDK发送消息
  const message = this.tim.createTextMessage({
    to: this.data.receiverId.toString(),
    conversationType: TIM.TYPES.CONV_C2C,
    payload: {
      text: content
    }
  });
  
  try {
    const res = await this.tim.sendMessage(message);
    console.log('消息发送成功', res);
  } catch (error) {
    console.error('消息发送失败', error);
    wx.showToast({
      title: '发送失败',
      icon: 'none'
    });
  }
}
```

#### 3.2.7 数据库表简化设计

使用第三方IM后，只需保留**防骚扰限制表**，消息存储由腾讯云IM负责：

```sql
-- 聊天限制表（防骚扰）
CREATE TABLE `t_chat_limit` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `sender_id` BIGINT NOT NULL COMMENT '发送者ID',
  `receiver_id` BIGINT NOT NULL COMMENT '接收者ID',
  `unreplied_count` INT DEFAULT 0 COMMENT '未回复消息数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_sender_receiver` (`sender_id`, `receiver_id`),
  KEY `idx_receiver` (`receiver_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='聊天限制表';
```

**对比原设计**：
- ✅ 不再需要 `t_chat_session` 和 `t_chat_message` 表
- ✅ 节省数据库存储和维护成本
- ✅ 消息历史、已读状态由IM SDK处理

#### 3.2.8 腾讯云IM回调配置

在腾讯云IM控制台配置回调地址：

**回调地址**：`https://yourdomain.com/api/chat/callback/after-send-msg`

**启用的回调**：
- 单聊消息发送后回调：用于防骚扰计数
- 单聊消息已读回调：用于清除限制

**回调鉴权**（可选）：
```java
@Component
public class TencentImCallbackValidator {
    
    @Autowired
    private TencentImConfig config;
    
    public boolean validate(HttpServletRequest request) {
        String timestamp = request.getHeader("X-TC-Timestamp");
        String signature = request.getHeader("X-TC-Signature");
        
        // 验证签名逻辑
        String expectedSig = generateSignature(timestamp, config.getSecretKey());
        return expectedSig.equals(signature);
    }
}
```

#### 3.2.9 费用说明

**腾讯云IM免费额度**（足够前期使用）：
- DAU（日活用户数）：100万以下免费
- 峰值在线：100万以下免费
- 群组数量：10万个免费
- 历史消息存储：7天免费

**付费方式**（后期扩展）：
- 按DAU收费：100万-500万DAU，约 ¥0.008/DAU/天
- 历史消息存储扩展：30天/90天可选

**💰 成本对比**：
- 自建系统：服务器 + 开发 + 运维 ≈ ¥5000+/月
- 第三方IM：前期免费，后期 ≈ ¥2000/月（10万DAU）

---

### 3.3 动态广场模块

#### 3.3.1 模块概述
**核心功能**: 用户发布和浏览动态，进行社交互动

**主要功能**:
- 发布动态（文字+图片+位置）
- 浏览附近动态
- 点赞、评论
- 动态审核

#### 3.3.2 技术实现设计

**Controller层**: `MomentController`
```java
@RestController
@RequestMapping("/api/moments")
public class MomentController {
    
    // 获取动态列表
    @GetMapping("/list")
    public Result<Page<MomentDTO>> getMoments(
        @RequestParam(required = false) Double latitude,
        @RequestParam(required = false) Double longitude,
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
    
    // 获取动态详情
    @GetMapping("/{momentId}")
    public Result<MomentDetailDTO> getMomentDetail(
        @PathVariable Long momentId,
        HttpServletRequest request
    );
    
    // 发布动态
    @PostMapping("/publish")
    public Result<MomentDTO> publishMoment(
        @RequestBody @Validated PublishMomentRequest request,
        HttpServletRequest httpRequest
    );
    
    // 点赞动态
    @PostMapping("/like")
    public Result<Void> likeMoment(
        @RequestBody @Validated LikeMomentRequest request,
        HttpServletRequest httpRequest
    );
    
    // 取消点赞
    @DeleteMapping("/unlike")
    public Result<Void> unlikeMoment(
        @RequestParam Long momentId,
        HttpServletRequest request
    );
    
    // 评论动态
    @PostMapping("/comment")
    public Result<CommentDTO> commentMoment(
        @RequestBody @Validated CommentRequest request,
        HttpServletRequest httpRequest
    );
    
    // 获取评论列表
    @GetMapping("/{momentId}/comments")
    public Result<Page<CommentDTO>> getComments(
        @PathVariable Long momentId,
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "20") Integer size
    );
    
    // 检查发布限制
    @GetMapping("/check-limit")
    public Result<PublishLimitDTO> checkPublishLimit(
        HttpServletRequest request
    );
}
```

**Service层**: `MomentService`
```java
@Service
public interface MomentService {
    
    /**
     * 发布动态
     * @param userId 用户ID
     * @param request 发布请求
     * @return 动态信息
     */
    MomentDTO publishMoment(Long userId, PublishMomentRequest request);
    
    /**
     * 获取动态列表（按距离排序）
     * @param userId 当前用户ID
     * @param latitude 纬度
     * @param longitude 经度
     * @param page 页码
     * @param size 每页数量
     * @return 动态列表
     */
    Page<MomentDTO> getMoments(Long userId, Double latitude, Double longitude, 
                               int page, int size);
    
    /**
     * 检查发布限制（每日3条）
     * @param userId 用户ID
     * @return 今日还可发布数量
     */
    int checkPublishLimit(Long userId);
    
    /**
     * 内容审核
     * @param content 动态内容
     * @return 审核结果
     */
    AuditResult auditContent(String content);
}
```

#### 3.3.3 内容审核机制

```java
@Service
public class ContentAuditService {
    
    @Autowired
    private SensitiveWordFilter sensitiveWordFilter;
    
    public AuditResult auditContent(String content, List<String> imageUrls) {
        // 1. 敏感词过滤
        if (sensitiveWordFilter.contains(content)) {
            return AuditResult.reject("内容包含敏感词");
        }
        
        // 2. 调用第三方内容审核API（腾讯云、阿里云）
        // 这里使用同步审核，小程序场景可以接受
        try {
            // 调用审核API
            ContentAuditResponse response = thirdPartyAuditService.audit(
                content, imageUrls
            );
            
            if (response.isPass()) {
                return AuditResult.pass();
            } else if (response.isReview()) {
                // 疑似违规，进入人工审核队列
                return AuditResult.review();
            } else {
                return AuditResult.reject(response.getReason());
            }
        } catch (Exception e) {
            log.error("内容审核失败", e);
            // 审核失败默认通过，避免影响用户体验
            // 可以异步进行二次审核
            return AuditResult.pass();
        }
    }
}
```

---

### 3.4 搭子平台模块

#### 3.4.1 模块概述
**核心功能**: 用户发布和参与搭子活动（运动、学习、旅游等）

**活动类型**:
- 运动搭子（羽毛球、篮球、跑步等）
- 学习搭子（图书馆、自习室）
- 旅游搭子（周末游、长途游）
- 健身搭子（健身房、瑜伽）
- 爱好搭子（摄影、音乐、手工等）

#### 3.4.2 技术实现设计

**Controller层**: `BuddyController`
```java
@RestController
@RequestMapping("/api/buddy")
public class BuddyController {
    
    // 获取搭子活动列表
    @GetMapping("/activities")
    public Result<Page<BuddyActivityDTO>> getActivities(
        @RequestParam(required = false) String type,  // all/sport/study/travel等
        @RequestParam(required = false) Double latitude,
        @RequestParam(required = false) Double longitude,
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
    
    // 获取活动详情
    @GetMapping("/activities/{activityId}")
    public Result<BuddyActivityDetailDTO> getActivityDetail(
        @PathVariable Long activityId,
        HttpServletRequest request
    );
    
    // 发布搭子活动
    @PostMapping("/publish")
    public Result<BuddyActivityDTO> publishActivity(
        @RequestBody @Validated PublishBuddyRequest request,
        HttpServletRequest httpRequest
    );
    
    // 报名活动
    @PostMapping("/register")
    public Result<Void> registerActivity(
        @RequestBody @Validated RegisterRequest request,
        HttpServletRequest httpRequest
    );
    
    // 取消报名
    @PostMapping("/cancel")
    public Result<Void> cancelRegistration(
        @RequestParam Long activityId,
        HttpServletRequest request
    );
    
    // 获取我发布的活动
    @GetMapping("/my-activities")
    public Result<Page<BuddyActivityDTO>> getMyActivities(
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
    
    // 获取我参与的活动
    @GetMapping("/my-registrations")
    public Result<Page<BuddyActivityDTO>> getMyRegistrations(
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
}
```

**Service层**: `BuddyService`
```java
@Service
public interface BuddyService {
    
    /**
     * 发布搭子活动
     * @param userId 发起人ID
     * @param request 发布请求
     * @return 活动信息
     */
    BuddyActivityDTO publishActivity(Long userId, PublishBuddyRequest request);
    
    /**
     * 报名活动
     * @param userId 用户ID
     * @param activityId 活动ID
     */
    void registerActivity(Long userId, Long activityId);
    
    /**
     * 检查报名条件
     * @param userId 用户ID
     * @param activityId 活动ID
     * @return 检查结果
     */
    RegistrationCheckResult checkRegistration(Long userId, Long activityId);
}
```

#### 3.4.3 活动状态管理

```java
活动状态枚举：
1 - 招募中（正常显示）
2 - 已满员（显示但不可报名）
3 - 已结束（不显示）
4 - 已取消（不显示）

状态转换逻辑：
- 发布时：状态=1（招募中）
- 人数满时：状态=2（已满员）
- 活动时间过期：定时任务更新为3（已结束）
- 发起人取消：状态=4（已取消）
```

---

### 3.5 线下活动模块

#### 3.5.1 模块概述
**核心功能**: 平台官方组织的线下活动，用户付费报名参加

**活动类型**:
- 单身派对
- 兴趣沙龙
- 户外拓展
- 读书分享会
等

#### 3.5.2 技术实现设计

**Controller层**: `OfflineActivityController`
```java
@RestController
@RequestMapping("/api/activities")
public class OfflineActivityController {
    
    // 获取官方活动列表
    @GetMapping("/official")
    public Result<Page<OfficialActivityDTO>> getOfficialActivities(
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size
    );
    
    // 获取活动详情
    @GetMapping("/{activityId}")
    public Result<OfficialActivityDetailDTO> getActivityDetail(
        @PathVariable Long activityId,
        HttpServletRequest request
    );
    
    // 报名活动
    @PostMapping("/register")
    public Result<ActivityRegistrationDTO> registerActivity(
        @RequestBody @Validated ActivityRegistrationRequest request,
        HttpServletRequest httpRequest
    );
    
    // 获取我的活动记录
    @GetMapping("/my-history")
    public Result<Page<ActivityRegistrationDTO>> getMyHistory(
        @RequestParam(defaultValue = "1") Integer page,
        @RequestParam(defaultValue = "10") Integer size,
        HttpServletRequest request
    );
}
```

---

### 3.6 会员与支付模块

#### 3.6.1 会员功能开关设计

**系统配置表设计**:
```sql
CREATE TABLE `t_system_config` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `config_key` VARCHAR(100) NOT NULL UNIQUE COMMENT '配置键',
  `config_value` VARCHAR(500) COMMENT '配置值',
  `config_desc` VARCHAR(200) COMMENT '配置描述',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) COMMENT='系统配置表';

-- 初始化配置
INSERT INTO `t_system_config` VALUES
(1, 'vip.feature.enabled', 'false', '会员功能开关：true-开启，false-关闭'),
(2, 'vip.daily.free.match.count', '3', '免费用户每日匹配次数'),
(3, 'vip.daily.free.super.like.count', '1', '免费用户每日超级喜欢次数');
```

**配置服务实现**:
```java
@Service
public class SystemConfigService {
    
    @Autowired
    private SystemConfigMapper configMapper;
    
    @Autowired
    private RedisTemplate<String, String> redisTemplate;
    
    private static final String CONFIG_CACHE_KEY = "system:config:";
    
    /**
     * 获取配置值（带缓存）
     * @param key 配置键
     * @param defaultValue 默认值
     * @return 配置值
     */
    public String getString(String key, String defaultValue) {
        // 先从Redis缓存读取
        String cacheKey = CONFIG_CACHE_KEY + key;
        String cachedValue = redisTemplate.opsForValue().get(cacheKey);
        
        if (cachedValue != null) {
            return cachedValue;
        }
        
        // 缓存未命中，从数据库读取
        SystemConfig config = configMapper.selectOne(
            new LambdaQueryWrapper<SystemConfig>()
                .eq(SystemConfig::getConfigKey, key)
        );
        
        String value = config != null ? config.getConfigValue() : defaultValue;
        
        // 写入缓存（10分钟过期）
        redisTemplate.opsForValue().set(cacheKey, value, 10, TimeUnit.MINUTES);
        
        return value;
    }
    
    public Boolean getBoolean(String key, Boolean defaultValue) {
        String value = getString(key, String.valueOf(defaultValue));
        return Boolean.parseBoolean(value);
    }
    
    public Integer getInt(String key, Integer defaultValue) {
        String value = getString(key, String.valueOf(defaultValue));
        return Integer.parseInt(value);
    }
    
    /**
     * 更新配置（清除缓存）
     * @param key 配置键
     * @param value 配置值
     */
    public void updateConfig(String key, String value) {
        configMapper.update(
            new SystemConfig().setConfigValue(value),
            new LambdaUpdateWrapper<SystemConfig>()
                .eq(SystemConfig::getConfigKey, key)
        );
        
        // 清除缓存
        redisTemplate.delete(CONFIG_CACHE_KEY + key);
    }
}
```

**会员功能控制**:
```java
@Service
public class VipFeatureService {
    
    @Autowired
    private SystemConfigService configService;
    
    @Autowired
    private VipService vipService;
    
    /**
     * 检查是否有权限使用某功能
     * @param userId 用户ID
     * @param feature 功能名称
     * @return 是否有权限
     */
    public boolean hasPermission(Long userId, String feature) {
        // 1. 检查会员功能开关
        boolean vipEnabled = configService.getBoolean("vip.feature.enabled", false);
        
        // 2. 如果会员功能关闭，所有用户都有权限
        if (!vipEnabled) {
            return true;
        }
        
        // 3. 会员功能开启，检查用户VIP状态
        VipMember vip = vipService.getValidVip(userId);
        if (vip != null) {
            return true;  // VIP用户有权限
        }
        
        // 4. 非VIP用户，检查该功能是否需要VIP
        return !isVipOnlyFeature(feature);
    }
    
    private boolean isVipOnlyFeature(String feature) {
        Set<String> vipOnlyFeatures = Set.of(
            "unlimited_match",      // 无限匹配
            "advanced_filter",      // 高级筛选
            "see_who_liked_me",     // 查看谁喜欢我
            "priority_display"      // 优先展示
        );
        return vipOnlyFeatures.contains(feature);
    }
}
```

#### 3.6.2 支付模块设计

**Controller层**: `PaymentController`
```java
@RestController
@RequestMapping("/api/payment")
public class PaymentController {
    
    // 创建VIP订单
    @PostMapping("/vip/create-order")
    public Result<OrderDTO> createVipOrder(
        @RequestBody @Validated CreateVipOrderRequest request,
        HttpServletRequest httpRequest
    );
    
    // 微信支付预下单
    @PostMapping("/wx/prepay")
    public Result<WxPayParamsDTO> wxPrepay(
        @RequestParam String orderNo,
        HttpServletRequest request
    );
    
    // 微信支付回调
    @PostMapping("/wx/callback")
    public String wxPayCallback(@RequestBody String requestBody);
    
    // 查询订单状态
    @GetMapping("/order/{orderNo}")
    public Result<OrderDTO> getOrder(
        @PathVariable String orderNo,
        HttpServletRequest request
    );
}
```

**Service层**: `PaymentService`
```java
@Service
public interface PaymentService {
    
    /**
     * 创建订单
     * @param userId 用户ID
     * @param orderType 订单类型
     * @param amount 金额
     * @return 订单信息
     */
    OrderDTO createOrder(Long userId, int orderType, BigDecimal amount);
    
    /**
     * 处理支付成功回调
     * @param orderNo 订单号
     * @param transactionId 第三方交易号
     */
    void handlePaymentSuccess(String orderNo, String transactionId);
    
    /**
     * 开通VIP会员
     * @param userId 用户ID
     * @param vipLevel 会员等级
     */
    void activateVip(Long userId, int vipLevel);
}
```

---

### 3.7 实名认证模块（去除学历认证）

#### 3.7.1 模块概述
**核心功能**: 
- ✅ 手机号验证
- ✅ 实名认证（身份证+人脸识别）
- ❌ 学历认证（已移除）

#### 3.7.2 技术实现设计

**Controller层**: `VerificationController`
```java
@RestController
@RequestMapping("/api/verification")
public class VerificationController {
    
    // 发送手机验证码
    @PostMapping("/phone/send-code")
    public Result<Void> sendPhoneCode(
        @RequestBody @Validated SendCodeRequest request,
        HttpServletRequest httpRequest
    );
    
    // 验证手机号
    @PostMapping("/phone/verify")
    public Result<Void> verifyPhone(
        @RequestBody @Validated VerifyPhoneRequest request,
        HttpServletRequest httpRequest
    );
    
    // 提交实名认证
    @PostMapping("/idcard/submit")
    public Result<VerificationDTO> submitIdCard(
        @RequestBody @Validated IdCardVerificationRequest request,
        HttpServletRequest httpRequest
    );
    
    // 获取人脸识别参数
    @GetMapping("/face/params")
    public Result<FaceRecognitionParamsDTO> getFaceParams(
        @RequestParam Long verificationId,
        HttpServletRequest request
    );
    
    // 提交人脸识别结果
    @PostMapping("/face/submit")
    public Result<Void> submitFaceResult(
        @RequestBody @Validated FaceResultRequest request,
        HttpServletRequest httpRequest
    );
    
    // 获取认证状态
    @GetMapping("/status")
    public Result<VerificationStatusDTO> getStatus(
        HttpServletRequest request
    );
}
```

**Service层**: `VerificationService`
```java
@Service
public interface VerificationService {
    
    /**
     * 发送手机验证码
     * @param phone 手机号
     * @return 是否发送成功
     */
    boolean sendSmsCode(String phone);
    
    /**
     * 验证手机号
     * @param userId 用户ID
     * @param phone 手机号
     * @param code 验证码
     */
    void verifyPhone(Long userId, String phone, String code);
    
    /**
     * 提交实名认证
     * @param userId 用户ID
     * @param realName 真实姓名
     * @param idCard 身份证号
     * @return 认证ID
     */
    Long submitIdCardVerification(Long userId, String realName, String idCard);
    
    /**
     * 完成实名认证
     * @param verificationId 认证ID
     * @param faceToken 人脸识别token
     */
    void completeVerification(Long verificationId, String faceToken);
}
```

---

## 4. 数据库设计

### 4.1 核心表结构

#### 4.1.1 用户相关表（已存在）
```sql
-- t_user: 用户基础信息表
-- t_wx_user: 微信用户信息表
-- t_user_profile: 用户详细资料表
-- t_user_album: 用户相册表
-- t_user_tags: 用户标签关联表
-- t_tag_library: 标签库表
-- t_user_privacy: 用户隐私设置表
```

#### 4.1.2 匹配相关表（待创建）
```sql
-- 1. 用户匹配偏好表
CREATE TABLE `t_matching_preference` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `min_age` INT COMMENT '期望最小年龄',
  `max_age` INT COMMENT '期望最大年龄',
  `min_height` INT COMMENT '期望最小身高(cm)',
  `max_height` INT COMMENT '期望最大身高(cm)',
  `education` VARCHAR(50) COMMENT '期望学历（多个用逗号分隔）',
  `income_range` VARCHAR(100) COMMENT '期望收入范围',
  `max_distance` INT DEFAULT 50 COMMENT '最大距离(km)',
  `location_latitude` DECIMAL(10,7) COMMENT '用户纬度',
  `location_longitude` DECIMAL(10,7) COMMENT '用户经度',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_location` (`location_latitude`, `location_longitude`)
) COMMENT='用户匹配偏好表';

-- 2. 匹配记录表
CREATE TABLE `t_match_record` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `from_user_id` BIGINT NOT NULL COMMENT '发起匹配的用户ID',
  `to_user_id` BIGINT NOT NULL COMMENT '被匹配的用户ID',
  `match_type` TINYINT NOT NULL COMMENT '匹配类型：1-普通喜欢，2-超级喜欢',
  `match_result` TINYINT DEFAULT 0 COMMENT '匹配结果：0-未匹配，1-单向喜欢，2-双向匹配成功',
  `matched_at` DATETIME COMMENT '匹配成功时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  UNIQUE KEY `uk_user_match` (`from_user_id`, `to_user_id`),
  KEY `idx_from_user` (`from_user_id`),
  KEY `idx_to_user` (`to_user_id`),
  KEY `idx_match_result` (`match_result`)
) COMMENT='匹配记录表';

-- 3. 匹配限制表
CREATE TABLE `t_match_limit` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `date` DATE NOT NULL COMMENT '日期',
  `free_match_count` INT DEFAULT 0 COMMENT '免费匹配次数（已使用）',
  `vip_match_count` INT DEFAULT 0 COMMENT 'VIP匹配次数（已使用）',
  `super_like_count` INT DEFAULT 0 COMMENT '超级喜欢次数（已使用）',
  `last_match_time` DATETIME COMMENT '最后匹配时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_user_date` (`user_id`, `date`),
  KEY `idx_date` (`date`)
) COMMENT='用户每日匹配限制表';
```

#### 4.1.3 聊天相关表（待创建）

**说明**：使用腾讯云IM第三方服务，消息存储由IM系统负责，后端只需维护防骚扰限制表。

```sql
-- 聊天限制表（防骚扰）
CREATE TABLE `t_chat_limit` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `sender_id` BIGINT NOT NULL COMMENT '发送者ID',
  `receiver_id` BIGINT NOT NULL COMMENT '接收者ID',
  `unreplied_count` INT DEFAULT 0 COMMENT '未回复消息数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_sender_receiver` (`sender_id`, `receiver_id`),
  KEY `idx_receiver` (`receiver_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='聊天限制表';
```

**优势**：
- ✅ 简化数据库设计，无需维护消息表和会话表
- ✅ 消息存储、多端同步、离线推送由腾讯云IM负责
- ✅ 降低服务器存储和运维成本
- ✅ 提供更稳定可靠的聊天服务

#### 4.1.4 动态相关表（待创建）
```sql
-- 1. 动态表
CREATE TABLE `t_moment` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL COMMENT '发布者ID',
  `content` VARCHAR(1000) COMMENT '动态内容',
  `location` VARCHAR(200) COMMENT '位置信息',
  `latitude` DECIMAL(10,7) COMMENT '纬度',
  `longitude` DECIMAL(10,7) COMMENT '经度',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `comment_count` INT DEFAULT 0 COMMENT '评论数',
  `view_count` INT DEFAULT 0 COMMENT '浏览数',
  `is_top` TINYINT DEFAULT 0 COMMENT '是否置顶：0-否，1-是',
  `top_expire_time` DATETIME COMMENT '置顶过期时间',
  `audit_status` TINYINT DEFAULT 0 COMMENT '审核状态：0-待审核，1-通过，2-拒绝',
  `audit_remark` VARCHAR(500) COMMENT '审核备注',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  KEY `idx_user_id` (`user_id`),
  KEY `idx_location` (`latitude`, `longitude`),
  KEY `idx_created` (`created_at`),
  KEY `idx_audit` (`audit_status`)
) COMMENT='动态表';

-- 2. 动态图片表
CREATE TABLE `t_moment_photo` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `moment_id` BIGINT NOT NULL COMMENT '动态ID',
  `photo_url` VARCHAR(500) NOT NULL COMMENT '图片URL',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY `idx_moment_id` (`moment_id`)
) COMMENT='动态图片表';

-- 3. 动态点赞表
CREATE TABLE `t_moment_like` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `moment_id` BIGINT NOT NULL COMMENT '动态ID',
  `user_id` BIGINT NOT NULL COMMENT '点赞用户ID',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_moment_user` (`moment_id`, `user_id`),
  KEY `idx_user_id` (`user_id`)
) COMMENT='动态点赞表';

-- 4. 动态评论表
CREATE TABLE `t_moment_comment` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `moment_id` BIGINT NOT NULL COMMENT '动态ID',
  `user_id` BIGINT NOT NULL COMMENT '评论用户ID',
  `content` VARCHAR(500) NOT NULL COMMENT '评论内容',
  `reply_to_user_id` BIGINT COMMENT '回复的用户ID',
  `reply_to_comment_id` BIGINT COMMENT '回复的评论ID',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  KEY `idx_moment_id` (`moment_id`),
  KEY `idx_user_id` (`user_id`)
) COMMENT='动态评论表';
```

#### 4.1.5 搭子活动表（待创建）
```sql
-- 1. 搭子活动表
CREATE TABLE `t_buddy_activity` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `creator_id` BIGINT NOT NULL COMMENT '发起人ID',
  `type` VARCHAR(20) NOT NULL COMMENT '活动类型：sport/study/travel/workout/hobby',
  `title` VARCHAR(100) NOT NULL COMMENT '活动标题',
  `description` VARCHAR(500) COMMENT '活动详情',
  `activity_time` DATETIME NOT NULL COMMENT '活动时间',
  `location` VARCHAR(200) NOT NULL COMMENT '活动地点',
  `latitude` DECIMAL(10,7) COMMENT '纬度',
  `longitude` DECIMAL(10,7) COMMENT '经度',
  `max_participants` INT DEFAULT 5 COMMENT '最大参与人数',
  `current_participants` INT DEFAULT 1 COMMENT '当前参与人数',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1-招募中，2-已满员，3-已结束，4-已取消',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  KEY `idx_creator` (`creator_id`),
  KEY `idx_type` (`type`),
  KEY `idx_location` (`latitude`, `longitude`),
  KEY `idx_time` (`activity_time`),
  KEY `idx_status` (`status`)
) COMMENT='搭子活动表';

-- 2. 搭子活动报名表
CREATE TABLE `t_buddy_registration` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `activity_id` BIGINT NOT NULL COMMENT '活动ID',
  `user_id` BIGINT NOT NULL COMMENT '报名用户ID',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1-已报名，2-已取消',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  UNIQUE KEY `uk_activity_user` (`activity_id`, `user_id`),
  KEY `idx_user_id` (`user_id`)
) COMMENT='搭子活动报名表';
```

#### 4.1.6 线下活动表（待创建）
```sql
-- 1. 官方活动表
CREATE TABLE `t_official_activity` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `title` VARCHAR(100) NOT NULL COMMENT '活动标题',
  `description` TEXT COMMENT '活动详情',
  `poster_url` VARCHAR(500) COMMENT '活动海报',
  `activity_time` DATETIME NOT NULL COMMENT '活动时间',
  `location` VARCHAR(200) NOT NULL COMMENT '活动地点',
  `latitude` DECIMAL(10,7) COMMENT '纬度',
  `longitude` DECIMAL(10,7) COMMENT '经度',
  `max_participants` INT DEFAULT 100 COMMENT '最大参与人数',
  `current_participants` INT DEFAULT 0 COMMENT '当前参与人数',
  `fee` DECIMAL(10,2) DEFAULT 0 COMMENT '活动费用（0表示免费）',
  `status` TINYINT DEFAULT 1 COMMENT '状态：1-报名中，2-已满员，3-已结束，4-已取消',
  `is_featured` TINYINT DEFAULT 0 COMMENT '是否精选',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  KEY `idx_time` (`activity_time`),
  KEY `idx_status` (`status`),
  KEY `idx_featured` (`is_featured`)
) COMMENT='官方活动表';

-- 2. 活动报名表
CREATE TABLE `t_activity_registration` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `activity_id` BIGINT NOT NULL COMMENT '活动ID',
  `user_id` BIGINT NOT NULL COMMENT '报名用户ID',
  `order_id` BIGINT COMMENT '订单ID（付费活动）',
  `participant_name` VARCHAR(50) COMMENT '参与者姓名',
  `phone` VARCHAR(20) COMMENT '联系电话',
  `emergency_contact` VARCHAR(100) COMMENT '紧急联系人',
  `status` TINYINT DEFAULT 0 COMMENT '状态：0-待支付，1-已报名，2-已取消',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  UNIQUE KEY `uk_activity_user` (`activity_id`, `user_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_order_id` (`order_id`)
) COMMENT='活动报名表';
```

#### 4.1.7 会员相关表（待创建）
```sql
-- 1. 会员信息表
CREATE TABLE `t_vip_member` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `vip_level` TINYINT DEFAULT 1 COMMENT '会员等级：1-月度，2-季度，3-年度',
  `start_time` DATETIME NOT NULL COMMENT '开始时间',
  `expire_time` DATETIME NOT NULL COMMENT '到期时间',
  `auto_renew` TINYINT DEFAULT 0 COMMENT '是否自动续费：0-否，1-是',
  `status` TINYINT DEFAULT 1 COMMENT '状态：0-已过期，1-有效',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_user_id` (`user_id`),
  KEY `idx_expire_time` (`expire_time`)
) COMMENT='会员信息表';

-- 2. 订单表
CREATE TABLE `t_order` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `order_no` VARCHAR(64) NOT NULL COMMENT '订单号',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `order_type` TINYINT NOT NULL COMMENT '订单类型：1-月度会员，2-季度会员，3-年度会员，4-查看喜欢我，5-超级喜欢，6-置顶动态，7-活动报名',
  `amount` DECIMAL(10,2) NOT NULL COMMENT '金额',
  `pay_status` TINYINT DEFAULT 0 COMMENT '支付状态：0-待支付，1-已支付，2-已退款',
  `pay_method` VARCHAR(20) COMMENT '支付方式：wxpay-微信支付',
  `transaction_id` VARCHAR(100) COMMENT '第三方交易号',
  `paid_at` DATETIME COMMENT '支付时间',
  `expire_at` DATETIME COMMENT '订单过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_pay_status` (`pay_status`)
) COMMENT='订单表';

-- 3. 系统配置表
CREATE TABLE `t_system_config` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `config_key` VARCHAR(100) NOT NULL COMMENT '配置键',
  `config_value` VARCHAR(500) COMMENT '配置值',
  `config_desc` VARCHAR(200) COMMENT '配置描述',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uk_config_key` (`config_key`)
) COMMENT='系统配置表';

-- 初始化配置
INSERT INTO `t_system_config` VALUES
(1, 'vip.feature.enabled', 'false', '会员功能开关：true-开启，false-关闭（前期关闭，全功能免费）'),
(2, 'vip.daily.free.match.count', '3', '免费用户每日匹配次数'),
(3, 'vip.daily.free.super.like.count', '1', '免费用户每日超级喜欢次数');
```

#### 4.1.8 认证相关表（待创建，不含学历认证）
```sql
-- 用户认证表
CREATE TABLE `t_user_verification` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `verify_type` TINYINT NOT NULL COMMENT '认证类型：1-实名认证，2-手机号认证',
  `phone` VARCHAR(20) COMMENT '手机号',
  `real_name` VARCHAR(50) COMMENT '真实姓名',
  `id_card` VARCHAR(18) COMMENT '身份证号（加密存储）',
  `verify_status` TINYINT DEFAULT 0 COMMENT '认证状态：0-未提交，1-审核中，2-通过，3-拒绝',
  `verify_remark` VARCHAR(500) COMMENT '审核备注',
  `verified_at` DATETIME COMMENT '认证通过时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted` TINYINT DEFAULT 0,
  KEY `idx_user_id` (`user_id`),
  KEY `idx_verify_type` (`verify_type`)
) COMMENT='用户认证表';
```

### 4.2 索引优化建议

#### 4.2.1 高频查询索引
```sql
-- 匹配推荐查询（距离+条件）
ALTER TABLE t_user_profile 
ADD INDEX idx_match_condition (gender, age, deleted);

-- 动态列表查询（距离+时间）
ALTER TABLE t_moment 
ADD INDEX idx_location_time (latitude, longitude, created_at, audit_status);

-- 搭子活动查询（类型+时间+状态）
ALTER TABLE t_buddy_activity 
ADD INDEX idx_type_time_status (type, activity_time, status, deleted);
```

#### 4.2.2 联合索引设计原则
1. 最左匹配原则
2. 区分度高的列放前面
3. 覆盖索引优化
4. 避免冗余索引

---

## 5. 接口设计

### 5.1 接口规范

#### 5.1.1 统一响应格式
```java
@Data
public class Result<T> {
    private Integer code;      // 状态码：200-成功，其他-失败
    private String message;    // 提示信息
    private T data;            // 返回数据
    private Long timestamp;    // 时间戳
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("success");
        result.setData(data);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
    
    public static <T> Result<T> error(Integer code, String message) {
        Result<T> result = new Result<>();
        result.setCode(code);
        result.setMessage(message);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
}
```

#### 5.1.2 分页响应格式
```java
@Data
public class PageResult<T> {
    private List<T> records;     // 数据列表
    private Long total;          // 总记录数
    private Integer current;     // 当前页
    private Integer size;        // 每页大小
    private Integer pages;       // 总页数
}
```

#### 5.1.3 错误码设计
```java
public class ErrorCode {
    // 系统级错误 (1000-1999)
    public static final int SYSTEM_ERROR = 1000;
    public static final int PARAM_ERROR = 1001;
    public static final int AUTH_ERROR = 1002;
    
    // 业务级错误 (2000-2999)
    public static final int USER_NOT_FOUND = 2001;
    public static final int MATCH_LIMIT_EXCEEDED = 2002;
    public static final int ALREADY_LIKED = 2003;
    public static final int NOT_MATCHED = 2004;
    public static final int SEND_MESSAGE_FORBIDDEN = 2005;
    public static final int PUBLISH_LIMIT_EXCEEDED = 2006;
    public static final int ACTIVITY_FULL = 2007;
    public static final int ALREADY_REGISTERED = 2008;
    
    // VIP相关错误 (3000-3999)
    public static final int VIP_REQUIRED = 3001;
    public static final int VIP_EXPIRED = 3002;
}
```

### 5.2 核心接口列表

#### 5.2.1 用户认证模块
```
POST   /api/auth/wx/login              微信登录
GET    /api/auth/user/info             获取用户信息
POST   /api/auth/token/refresh         刷新Token
```

#### 5.2.2 用户资料模块
```
POST   /api/profile/complete           完善资料
PUT    /api/profile/update             更新资料
GET    /api/profile/{userId}           查看资料
POST   /api/profile/tags               保存标签
GET    /api/tags/library               获取标签库
POST   /api/profile/photos             上传照片
GET    /api/profile/preference         获取匹配偏好
POST   /api/profile/preference         保存匹配偏好
```

#### 5.2.3 匹配系统模块
```
GET    /api/matching/recommend         获取推荐用户
POST   /api/matching/like              喜欢操作
POST   /api/matching/super-like        超级喜欢
POST   /api/matching/pass              跳过操作
GET    /api/matching/liked-me          查看谁喜欢我
```

#### 5.2.4 聊天通讯模块
```
GET    /api/chat/sessions              获取聊天列表
GET    /api/chat/messages              获取聊天消息
POST   /api/chat/send                  发送消息
POST   /api/chat/read                  标记已读
GET    /api/chat/unread-count          未读消息数
```

#### 5.2.5 动态广场模块
```
GET    /api/moments/list               获取动态列表
GET    /api/moments/{momentId}         获取动态详情
POST   /api/moments/publish            发布动态
POST   /api/moments/like               点赞动态
DELETE /api/moments/unlike             取消点赞
POST   /api/moments/comment            评论动态
GET    /api/moments/{id}/comments      获取评论
GET    /api/moments/check-limit        检查发布限制
```

#### 5.2.6 搭子平台模块
```
GET    /api/buddy/activities           获取活动列表
GET    /api/buddy/activities/{id}      获取活动详情
POST   /api/buddy/publish              发布活动
POST   /api/buddy/register             报名活动
POST   /api/buddy/cancel               取消报名
GET    /api/buddy/my-activities        我发布的活动
GET    /api/buddy/my-registrations     我参与的活动
```

#### 5.2.7 线下活动模块
```
GET    /api/activities/official        获取官方活动
GET    /api/activities/{activityId}    获取活动详情
POST   /api/activities/register        报名活动
GET    /api/activities/my-history      我的活动记录
```

#### 5.2.8 会员与支付模块
```
GET    /api/vip/info                   获取会员信息
POST   /api/vip/create-order           创建VIP订单
POST   /api/payment/wx/prepay          微信支付预下单
POST   /api/payment/wx/callback        微信支付回调
GET    /api/payment/order/{orderNo}    查询订单状态
```

#### 5.2.9 认证模块（不含学历）
```
POST   /api/verification/phone/send-code    发送验证码
POST   /api/verification/phone/verify       验证手机号
POST   /api/verification/idcard/submit      提交实名认证
GET    /api/verification/face/params        获取人脸识别参数
POST   /api/verification/face/submit        提交人脸识别结果
GET    /api/verification/status             获取认证状态
```

#### 5.2.10 系统配置模块（管理端）
```
GET    /api/admin/config                   获取配置列表
PUT    /api/admin/config/{key}             更新配置
POST   /api/admin/config/vip/toggle        切换会员功能开关
```

---

## 6. 会员功能开关设计

### 6.1 设计目标
- **前期**：会员功能关闭，所有用户享受全功能，快速积累用户
- **后期**：运营稳定后开启会员功能，实现商业变现
- **切换**：通过配置表一键切换，无需修改代码

### 6.2 实现方案

#### 6.2.1 配置表设计
```sql
-- 系统配置表
CREATE TABLE `t_system_config` (
  `config_key` VARCHAR(100) PRIMARY KEY,
  `config_value` VARCHAR(500),
  `config_desc` VARCHAR(200)
);

-- 关键配置项
INSERT INTO t_system_config VALUES
('vip.feature.enabled', 'false', '会员功能总开关'),
('vip.daily.free.match.count', '3', '免费用户每日匹配次数'),
('vip.daily.free.super.like.count', '1', '免费用户每日超级喜欢次数');
```

#### 6.2.2 配置服务
```java
@Service
public class SystemConfigService {
    
    @Autowired
    private SystemConfigMapper configMapper;
    
    @Autowired
    private RedisTemplate<String, String> redisTemplate;
    
    /**
     * 获取会员功能开关状态
     * @return true-开启，false-关闭
     */
    public boolean isVipFeatureEnabled() {
        return getBoolean("vip.feature.enabled", false);
    }
    
    /**
     * 切换会员功能开关
     * @param enabled 是否开启
     */
    @Transactional
    public void toggleVipFeature(boolean enabled) {
        updateConfig("vip.feature.enabled", String.valueOf(enabled));
        
        // 清除相关缓存
        clearVipCache();
        
        // 记录操作日志
        log.info("会员功能开关已{}，操作时间：{}", 
                enabled ? "开启" : "关闭", LocalDateTime.now());
    }
    
    // 获取配置值（带缓存）
    private Boolean getBoolean(String key, Boolean defaultValue) {
        String cacheKey = "config:" + key;
        String cached = redisTemplate.opsForValue().get(cacheKey);
        
        if (cached != null) {
            return Boolean.parseBoolean(cached);
        }
        
        SystemConfig config = configMapper.selectById(key);
        String value = config != null ? config.getConfigValue() 
                                      : String.valueOf(defaultValue);
        
        // 缓存10分钟
        redisTemplate.opsForValue().set(cacheKey, value, 10, TimeUnit.MINUTES);
        
        return Boolean.parseBoolean(value);
    }
}
```

#### 6.2.3 业务逻辑中的应用

**匹配限制检查**:
```java
@Service
public class MatchingService {
    
    @Autowired
    private SystemConfigService configService;
    
    public boolean checkMatchLimit(Long userId, int matchType) {
        // 1. 检查会员功能开关
        if (!configService.isVipFeatureEnabled()) {
            return true;  // 会员功能关闭，不限制
        }
        
        // 2. 会员功能开启，检查VIP状态
        VipMember vip = vipService.getValidVip(userId);
        if (vip != null) {
            return true;  // VIP用户不限制
        }
        
        // 3. 非VIP用户，检查每日限制
        // ... 检查每日次数
    }
}
```

**查看喜欢我的人**:
```java
@Service
public class MatchingService {
    
    public Page<UserCardDTO> getLikedMe(Long userId, int page, int size) {
        // 1. 检查权限
        if (configService.isVipFeatureEnabled()) {
            // 会员功能开启，需要VIP权限
            VipMember vip = vipService.getValidVip(userId);
            if (vip == null) {
                throw new BusinessException(ErrorCode.VIP_REQUIRED, 
                    "该功能需要开通VIP会员");
            }
        }
        
        // 2. 查询喜欢我的人
        // ...
    }
}
```

### 6.3 管理端接口

**切换会员功能开关**:
```java
@RestController
@RequestMapping("/api/admin")
public class AdminConfigController {
    
    @Autowired
    private SystemConfigService configService;
    
    /**
     * 切换会员功能开关
     * @param enabled 是否开启
     */
    @PostMapping("/config/vip/toggle")
    public Result<Void> toggleVipFeature(@RequestParam Boolean enabled) {
        // 权限校验（管理员）
        // ...
        
        configService.toggleVipFeature(enabled);
        
        return Result.success(null);
    }
    
    /**
     * 获取会员功能状态
     */
    @GetMapping("/config/vip/status")
    public Result<VipFeatureStatusDTO> getVipFeatureStatus() {
        boolean enabled = configService.isVipFeatureEnabled();
        
        VipFeatureStatusDTO status = new VipFeatureStatusDTO();
        status.setEnabled(enabled);
        status.setFreeMatchCount(configService.getInt("vip.daily.free.match.count", 3));
        status.setFreeSuperLikeCount(configService.getInt("vip.daily.free.super.like.count", 1));
        
        return Result.success(status);
    }
}
```

### 6.4 切换策略

#### 6.4.1 开启会员功能前的准备
1. **数据分析**: 分析用户活跃度、留存率、付费意愿
2. **灰度测试**: 先对部分用户开启，观察反馈
3. **通知用户**: 提前通知用户会员功能即将开启
4. **优惠活动**: 首批会员优惠，吸引用户购买

#### 6.4.2 开启后的监控
1. **转化率监控**: 免费用户→付费用户转化率
2. **流失率监控**: 开启后的用户流失情况
3. **收入监控**: 每日、每月会员收入
4. **反馈收集**: 用户对会员功能的反馈

---

## 7. 关键业务流程与逻辑图

### 7.1 用户注册与资料完善流程

```
┌──────────┐
│ 用户打开  │
│   小程序  │
└─────┬────┘
      │
      ↓
┌──────────────────┐
│ 1.微信授权登录    │
│  • 获取code      │
│  • 换取openid    │
│  • 生成userId    │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 2.检查资料状态    │
│ profileComplete? │
└─────┬────────────┘
      │
      ├─[完善]──→ 进入首页
      │
      └─[未完善]
            ↓
      ┌──────────────────┐
      │ 3.引导完善资料    │
      │  • 基础信息       │
      │  • 上传头像       │
      │  • 选择标签       │
      │  • 设置匹配偏好   │
      └─────┬────────────┘
            │
            ↓
      ┌──────────────────┐
      │ 4.资料审核（自动）│
      │ profileComplete=1│
      └─────┬────────────┘
            │
            ↓
      ┌──────────────────┐
      │ 5.进入首页        │
      │  • 开始匹配       │
      └──────────────────┘
```

---

### 7.2 智能匹配与互动流程

```
┌──────────┐
│ 首页推荐  │
└─────┬────┘
      │
      ↓
┌─────────────────────────┐
│ 1.智能匹配算法           │
│  • 地理位置（30%权重）   │
│  • 兴趣标签（25%权重）   │
│  • 年龄偏好（20%权重）   │
│  • 基础信息（15%权重）   │
│  • 活跃度（10%权重）     │
└─────┬───────────────────┘
      │
      ↓
┌──────────────────┐
│ 2.展示推荐用户    │
└─────┬────────────┘
      │
      ├─[喜欢]──┐
      │         ↓
      │    ┌─────────────┐
      │    │ 3.保存喜欢记录│
      │    └─────┬───────┘
      │          │
      │          ├─[对方也喜欢你]
      │          │      ↓
      │          │ ┌─────────────┐
      │          │ │ 4.匹配成功！ │
      │          │ │ • 发送通知   │
      │          │ │ • 开启聊天   │
      │          │ └─────────────┘
      │          │
      │          └─[对方未喜欢]
      │                 ↓
      │            ┌─────────────┐
      │            │ 等待对方喜欢 │
      │            └─────────────┘
      │
      └─[跳过]──→ 下一个推荐
```

---

### 7.3 动态广场发布流程

```
┌──────────┐
│ 用户点击  │
│ 发布动态  │
└─────┬────┘
      │
      ↓
┌──────────────────┐
│ 1.选择动态类型    │
│  • 搭子动态(1)   │
│  • 恋爱动态(2)   │
│  • 搭子活动(3)   │
│  • 官方活动(4)   │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 2.填写内容        │
│  • 文字内容       │
│  • 上传图片       │
│  • 添加位置       │
└─────┬────────────┘
      │
      ├─[活动类型]
      │      ↓
      │ ┌──────────────────┐
      │ │ 3.填写活动信息    │
      │ │  • 活动标题       │
      │ │  • 活动时间       │
      │ │  • 活动地点       │
      │ │  • 参与人数       │
      │ │  • 活动费用       │
      │ └─────┬────────────┘
      │       │
      └───────┘
            │
            ↓
      ┌──────────────────┐
      │ 4.检查发布限制    │
      │  • 普通用户: 3次/天│
      │  • VIP用户: 10次/天│
      └─────┬────────────┘
            │
            ├─[超过限制]──→ 提示升级VIP
            │
            └─[未超过]
                  ↓
            ┌──────────────────┐
            │ 5.内容审核（自动）│
            │  • 文本审核       │
            │  • 图片审核       │
            └─────┬────────────┘
                  │
                  ├─[审核通过]
                  │      ↓
                  │ ┌──────────────────┐
                  │ │ 6.发布成功        │
                  │ │  • 保存数据       │
                  │ │  • 推送到广场     │
                  │ └──────────────────┘
                  │
                  └─[审核不通过]
                        ↓
                   ┌──────────────────┐
                   │ 提示违规内容      │
                   └──────────────────┘
```

---

### 7.4 活动报名与管理流程

```
┌──────────┐
│ 用户浏览  │
│ 活动详情  │
└─────┬────┘
      │
      ↓
┌──────────────────┐
│ 1.检查活动状态    │
└─────┬────────────┘
      │
      ├─[招募中(1)]──→ 可以报名
      ├─[已满员(2)]──→ 不能报名
      ├─[已结束(3)]──→ 不能报名
      └─[已取消(4)]──→ 不能报名
      │
      ↓
┌──────────────────┐
│ 2.点击报名        │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 3.检查重复报名    │
└─────┬────────────┘
      │
      ├─[已报名]──→ 提示不能重复
      │
      └─[未报名]
            ↓
      ┌──────────────────┐
      │ 4.检查人数限制    │
      └─────┬────────────┘
            │
            ├─[已满员]──→ 提示满员
            │
            └─[有空位]
                  ↓
            ┌──────────────────┐
            │ 5.创建报名记录    │
            │  • 保存报名信息   │
            │  • 更新参与人数   │
            └─────┬────────────┘
                  │
                  ↓
            ┌──────────────────┐
            │ 6.判断是否满员    │
            │ current >= max?  │
            └─────┬────────────┘
                  │
                  ├─[是]──→ 更新状态为"已满员"
                  │
                  └─[否]──→ 保持"招募中"
                        │
                        ↓
                  ┌──────────────────┐
                  │ 7.通知发起人      │
                  │  • 有新人报名     │
                  └──────────────────┘
```

---

### 7.5 VIP支付流程

```
┌──────────┐
│ 用户选择  │
│ VIP套餐  │
└─────┬────┘
      │
      ↓
┌──────────────────┐
│ 1.选择会员等级    │
│  • 月度: ¥29.9   │
│  • 季度: ¥79.9   │
│  • 年度: ¥299.9  │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 2.创建订单        │
│  • 生成订单号     │
│  • 保存订单记录   │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 3.调用微信支付    │
│  • 统一下单接口   │
│  • 获取prepay_id  │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 4.返回支付参数    │
│  • timeStamp      │
│  • nonceStr       │
│  • package        │
│  • paySign        │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 5.小程序调起支付  │
│ wx.requestPayment │
└─────┬────────────┘
      │
      ├─[取消支付]──→ 订单状态保持"待支付"
      │
      └─[完成支付]
            ↓
      ┌──────────────────┐
      │ 6.微信发送回调    │
      │ POST /wx-notify   │
      └─────┬────────────┘
            │
            ↓
      ┌──────────────────┐
      │ 7.验证回调        │
      │  • 验证签名       │
      │  • 验证金额       │
      │  • 检查订单状态   │
      └─────┬────────────┘
            │
            ├─[验证失败]──→ 返回失败
            │
            └─[验证成功]
                  ↓
            ┌──────────────────┐
            │ 8.处理订单        │
            │  • 更新订单状态   │
            │  • 记录交易号     │
            └─────┬────────────┘
                  │
                  ↓
            ┌──────────────────┐
            │ 9.开通VIP         │
            │  • 查询现有会员   │
            │  • 计算到期时间   │
            │  • 更新会员记录   │
            └─────┬────────────┘
                  │
                  ↓
            ┌──────────────────┐
            │ 10.支付完成       │
            │  • VIP已激活      │
            │  • 刷新用户信息   │
            └──────────────────┘
```

---

### 7.6 实名认证流程

```
┌──────────┐
│ 用户申请  │
│ 实名认证  │
└─────┬────┘
      │
      ↓
┌──────────────────┐
│ 1.填写真实信息    │
│  • 真实姓名       │
│  • 身份证号       │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 2.上传证件照      │
│  • 身份证正面     │
│  • 身份证反面     │
│  • 手持证件照     │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 3.提交审核        │
│ status=0(待审核) │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 4.调用实名认证API │
│  • 身份证OCR      │
│  • 身份验证       │
│  • 人脸识别       │
└─────┬────────────┘
      │
      ├─[认证成功]
      │      ↓
      │ ┌──────────────────┐
      │ │ 5.更新认证状态    │
      │ │ status=1(通过)   │
      │ │  • 提取生日       │
      │ │  • 计算年龄       │
      │ │  • 更新用户信息   │
      │ └─────┬────────────┘
      │       │
      │       ↓
      │ ┌──────────────────┐
      │ │ 6.显示认证标识    │
      │ │  • 资料显示✓标记 │
      │ │  • 提升匹配权重   │
      │ └──────────────────┘
      │
      └─[认证失败]
            ↓
      ┌──────────────────┐
      │ 7.拒绝并说明原因  │
      │ status=2(拒绝)   │
      │  • 可重新提交     │
      └──────────────────┘
```

---

### 7.7 图片管理流程（两步上传）

```
┌──────────┐
│ 用户选择  │
│   图片   │
└─────┬────┘
      │
      ↓
┌──────────────────┐
│ 【第1步】         │
│ 上传到OSS         │
│  • POST /oss/upload│
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 1.检查文件合法性  │
│  • 格式(jpg/png)  │
│  • 大小(≤10MB)    │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 2.生成对象键      │
│ uploads/{type}/   │
│ {userId}/{date}/  │
│ {filename}        │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 3.上传到OSS       │
│  • 永久有效URL    │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 4.返回图片URL     │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 【第2步】         │
│ 保存到业务        │
│ POST /media/save  │
└─────┬────────────┘
      │
      ↓
┌──────────────────┐
│ 5.检查媒体类型    │
└─────┬────────────┘
      │
      ├─[avatar/background]
      │  (唯一类型)
      │      ↓
      │ ┌──────────────────┐
      │ │ 6.智能替换逻辑    │
      │ │  • 查询是否已存在 │
      │ │  • 存在→更新URL   │
      │ │  • 不存在→新建    │
      │ └──────────────────┘
      │
      └─[album/moment/activity]
         (多图类型)
            ↓
      ┌──────────────────┐
      │ 7.检查数量限制    │
      │  • 普通用户: 9张  │
      │  • VIP用户: 18张  │
      └─────┬────────────┘
            │
            ├─[超过限制]──→ 返回错误
            │
            └─[未超过]
                  ↓
            ┌──────────────────┐
            │ 8.保存图片记录    │
            │  • 创建media记录  │
            │  • 设置排序       │
            └─────┬────────────┘
                  │
                  ↓
            ┌──────────────────┐
            │ 9.返回成功        │
            └──────────────────┘
```

---

## 7.8 原有业务流程说明

### 7.8.1 用户首次登录完整流程
```
1. 用户打开小程序
2. 调用wx.login()获取code
3. 后端通过code获取openid
4. 查询数据库，判断是否新用户
5. 如果是新用户：
   - 创建用户记录
   - 生成JWT token
   - 返回isNewUser=true, profileComplete=false
6. 前端跳转资料完善页
7. 用户填写资料并上传照片
8. 用户选择兴趣标签
9. 用户设置匹配偏好
10. 查看引导页
11. 进入主页开始使用
```

### 7.8.2 匹配成功到聊天流程
```
1. 用户A喜欢用户B
2. 插入匹配记录：match_result = 1（单向喜欢）
3. 检查用户B是否也喜欢了用户A
4. 如果双向喜欢：
   - 更新两条记录：match_result = 2（匹配成功）
   - 清除聊天限制记录 t_chat_limit（如有）
   - 发送通知给双方："匹配成功！"
5. 前端显示匹配成功动画
6. 用户可以选择立即聊天或继续匹配
7. 点击聊天后，使用腾讯云IM SDK进入聊天页面
8. 双方可以自由发送消息（无限制，由IM系统处理）
```

### 7.8.3 发布动态完整流程
```
1. 用户点击"发布动态"
2. 编辑内容（文字/图片）
3. 选择位置（可选）
4. 点击"发布"
5. 检查每日发布限制（3条）
6. 上传图片到OSS
7. 调用内容审核API
8. 如果审核通过：
   - 保存动态到数据库
   - 立即在动态广场显示
9. 如果需要人工审核：
   - 保存动态，标记为待审核
   - 提示"动态审核中"
10. 审核通过后发送通知
11. 其他用户可以看到并互动
```

### 7.8.4 搭子活动报名流程
```
1. 用户浏览搭子活动列表
2. 点击活动查看详情
3. 点击"立即报名"
4. 检查报名条件：
   - 是否已报名
   - 是否已满员
   - 是否已过期
5. 插入报名记录
6. 更新活动参与人数
7. 创建与发起人的聊天会话
8. 发送通知给发起人
9. 如果满员，更新活动状态为"已满员"
10. 用户可以联系发起人沟通详情
```

### 7.8.5 会员购买支付流程
```
1. 用户进入会员中心
2. 选择会员套餐（月度/季度/年度）
3. 点击"购买"
4. 创建订单
5. 跳转到支付页面
6. 调用微信支付API
7. 用户完成支付
8. 微信回调后端
9. 验证支付结果
10. 开通会员权益
11. 发送通知："会员开通成功"
12. 用户可以使用会员特权
```

---

## 8. 数据字典

### 8.1 用户相关表

#### t_user (用户基础表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| openid | VARCHAR(100) | 微信openid | oXxxx123 |
| nickname | VARCHAR(50) | 昵称 | 小雨 |
| avatar | VARCHAR(500) | 头像URL | https://... |
| gender | TINYINT | 性别 1-男 2-女 | 2 |
| age | INT | 年龄 | 25 |
| phone | VARCHAR(20) | 手机号 | 138****8888 |
| profile_complete | TINYINT | 资料是否完善 | 1 |
| online_status | TINYINT | 在线状态 | 1 |
| last_login_time | DATETIME | 最后登录时间 | 2024-01-01 10:00:00 |
| created_at | DATETIME | 创建时间 | 2024-01-01 09:00:00 |
| updated_at | DATETIME | 更新时间 | 2024-01-01 10:00:00 |
| deleted | TINYINT | 逻辑删除 | 0 |

#### t_user_profile (用户详细资料表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| user_id | BIGINT | 用户ID | 1 |
| height | INT | 身高(cm) | 170 |
| weight | INT | 体重(kg) | 55 |
| education | VARCHAR(50) | 学历 | 本科 |
| occupation | VARCHAR(100) | 职业 | 设计师 |
| income | VARCHAR(50) | 收入 | 10-20万 |
| hometown | VARCHAR(200) | 家乡 | 北京市朝阳区 |
| current_city | VARCHAR(200) | 现居地 | 北京市朝阳区 |
| marital_status | VARCHAR(20) | 婚姻状况 | 未婚 |
| bio | VARCHAR(500) | 个人简介 | 热爱生活... |
| is_verified | TINYINT | 是否实名认证 | 1 |
| verify_status | TINYINT | 认证状态 | 2 |

### 8.2 匹配相关表

#### t_matching_preference (匹配偏好表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| user_id | BIGINT | 用户ID | 1 |
| min_age | INT | 最小年龄 | 22 |
| max_age | INT | 最大年龄 | 30 |
| min_height | INT | 最小身高 | 165 |
| max_height | INT | 最大身高 | 185 |
| education | VARCHAR(100) | 期望学历 | 本科,硕士 |
| income_range | VARCHAR(100) | 期望收入 | 10-20万,20-50万 |
| max_distance | INT | 最大距离(km) | 50 |
| location_latitude | DECIMAL(10,7) | 用户纬度 | 39.9042 |
| location_longitude | DECIMAL(10,7) | 用户经度 | 116.4074 |

#### t_match_record (匹配记录表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| from_user_id | BIGINT | 发起用户ID | 1 |
| to_user_id | BIGINT | 目标用户ID | 2 |
| match_type | TINYINT | 匹配类型 1-喜欢 2-超级喜欢 | 1 |
| match_result | TINYINT | 匹配结果 0-未匹配 1-单向 2-双向 | 2 |
| matched_at | DATETIME | 匹配成功时间 | 2024-01-01 10:30:00 |

### 8.3 聊天相关表

#### t_chat_limit (聊天限制表)

**说明**：使用腾讯云IM后，只需此表记录防骚扰限制，消息数据由IM系统管理

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| sender_id | BIGINT | 发送者ID | 1 |
| receiver_id | BIGINT | 接收者ID | 2 |
| unreplied_count | INT | 未回复消息数 | 1 |
| created_at | DATETIME | 创建时间 | 2024-01-01 10:00:00 |
| updated_at | DATETIME | 更新时间 | 2024-01-01 10:30:00 |

### 8.4 动态相关表

#### t_moment (动态表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| user_id | BIGINT | 发布者ID | 1 |
| content | VARCHAR(1000) | 动态内容 | 今天的夕阳... |
| location | VARCHAR(200) | 位置 | 朝阳公园 |
| latitude | DECIMAL(10,7) | 纬度 | 39.9042 |
| longitude | DECIMAL(10,7) | 经度 | 116.4074 |
| like_count | INT | 点赞数 | 12 |
| comment_count | INT | 评论数 | 3 |
| view_count | INT | 浏览数 | 100 |
| is_top | TINYINT | 是否置顶 | 0 |
| top_expire_time | DATETIME | 置顶过期时间 | NULL |
| audit_status | TINYINT | 审核状态 0-待审核 1-通过 2-拒绝 | 1 |

### 8.5 搭子活动相关表

#### t_buddy_activity (搭子活动表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| creator_id | BIGINT | 发起人ID | 1 |
| type | VARCHAR(50) | 活动类型 | sport |
| title | VARCHAR(100) | 活动标题 | 周末羽毛球 |
| activity_time | DATETIME | 活动时间 | 2024-01-20 14:00:00 |
| location | VARCHAR(200) | 地点 | 朝阳体育馆 |
| latitude | DECIMAL(10,7) | 纬度 | 39.9042 |
| longitude | DECIMAL(10,7) | 经度 | 116.4074 |
| max_participants | INT | 最大人数 | 5 |
| current_participants | INT | 当前人数 | 3 |
| description | VARCHAR(1000) | 活动详情 | 周末想打... |
| status | TINYINT | 状态 1-招募中 2-已满员 3-已结束 | 1 |

### 8.6 会员相关表

#### t_vip_member (会员信息表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| user_id | BIGINT | 用户ID | 1 |
| vip_level | TINYINT | 会员等级 1-月 2-季 3-年 | 2 |
| start_time | DATETIME | 开始时间 | 2024-01-01 10:00:00 |
| expire_time | DATETIME | 到期时间 | 2024-04-01 10:00:00 |
| auto_renew | TINYINT | 自动续费 | 0 |
| status | TINYINT | 状态 0-过期 1-有效 | 1 |

#### t_order (订单表)
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 主键 | 1 |
| order_no | VARCHAR(64) | 订单号 | VIP202401... |
| user_id | BIGINT | 用户ID | 1 |
| order_type | TINYINT | 订单类型 1-月度会员... | 2 |
| amount | DECIMAL(10,2) | 金额 | 79.90 |
| pay_status | TINYINT | 支付状态 0-待支付 1-已支付 | 1 |
| pay_method | VARCHAR(20) | 支付方式 | wxpay |
| transaction_id | VARCHAR(100) | 交易号 | 4200001234... |
| paid_at | DATETIME | 支付时间 | 2024-01-01 10:05:00 |

---

## 9. 性能优化方案

### 9.1 数据库优化

#### 9.1.1 索引策略
```sql
-- 匹配记录表索引
CREATE INDEX idx_from_user ON t_match_record(from_user_id, created_at);
CREATE INDEX idx_to_user ON t_match_record(to_user_id, match_result);
CREATE INDEX idx_match_result ON t_match_record(match_result, matched_at);

-- 聊天限制表索引（使用第三方IM，无需消息表索引）
-- 已在表定义中创建 UNIQUE KEY uk_sender_receiver 和 KEY idx_receiver

-- 动态表索引
CREATE INDEX idx_location ON t_moment(latitude, longitude, created_at);
CREATE INDEX idx_user_time ON t_moment(user_id, created_at);

-- 搭子活动表索引
CREATE INDEX idx_type_time ON t_buddy_activity(type, activity_time, status);
CREATE INDEX idx_location_time ON t_buddy_activity(latitude, longitude, activity_time);
```

#### 9.1.2 分库分表策略
```
当数据量达到一定规模时：

水平分表：
- t_moment: 按user_id取模，分8张表（动态内容较多）
- t_match_record: 按from_user_id取模，分8张表（匹配记录较多）
- t_buddy_activity: 按creator_id取模，分4张表（活动数据）

注：使用腾讯云IM后，无需对聊天表分表

读写分离：
- 主库：处理写操作
- 从库：处理读操作（多个）
- 使用MyCat或ShardingSphere实现
```

### 9.2 缓存策略

#### 9.2.1 Redis缓存设计
```java
/**
 * 缓存Key设计规范
 */
public class CacheKeys {
    // 用户信息缓存（30分钟）
    public static final String USER_INFO = "user:info:%s";
    
    // 用户匹配偏好（1小时）
    public static final String USER_PREFERENCE = "user:preference:%s";
    
    // 推荐用户列表（10分钟）
    public static final String RECOMMEND_USERS = "recommend:users:%s:%s";
    
    // 聊天会话列表（5分钟）
    public static final String CHAT_SESSIONS = "chat:sessions:%s";
    
    // 动态列表（3分钟）
    public static final String MOMENT_LIST = "moment:list:%s:%s";
    
    // 系统配置（永久，手动刷新）
    public static final String SYSTEM_CONFIG = "system:config:%s";
    
    // 会员功能开关（永久）
    public static final String VIP_FEATURE_ENABLED = "vip:feature:enabled";
    
    // 用户每日匹配次数（24小时）
    public static final String USER_MATCH_COUNT = "user:match:count:%s:%s";
}

/**
 * 缓存更新策略
 */
@Service
public class CacheService {
    
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    
    /**
     * 用户信息缓存：旁路缓存（Cache Aside）
     */
    public UserDTO getUserInfo(Long userId) {
        String key = String.format(CacheKeys.USER_INFO, userId);
        
        // 1. 先查缓存
        UserDTO cached = (UserDTO) redisTemplate.opsForValue().get(key);
        if (cached != null) {
            return cached;
        }
        
        // 2. 查数据库
        User user = userMapper.selectById(userId);
        UserDTO dto = convertToDTO(user);
        
        // 3. 写入缓存
        redisTemplate.opsForValue().set(key, dto, 30, TimeUnit.MINUTES);
        
        return dto;
    }
    
    /**
     * 更新用户信息：先更新数据库，再删除缓存
     */
    public void updateUserInfo(Long userId, UserDTO dto) {
        // 1. 更新数据库
        userMapper.updateById(convertToEntity(dto));
        
        // 2. 删除缓存
        String key = String.format(CacheKeys.USER_INFO, userId);
        redisTemplate.delete(key);
    }
    
    /**
     * 推荐用户列表：预热缓存
     */
    @Scheduled(cron = "0 */10 * * * ?")  // 每10分钟刷新
    public void refreshRecommendCache() {
        // 为活跃用户预热推荐列表
        List<Long> activeUserIds = getActiveUserIds();
        
        for (Long userId : activeUserIds) {
            List<UserCardDTO> recommendations = calculateRecommendations(userId);
            String key = String.format(CacheKeys.RECOMMEND_USERS, userId, 1);
            redisTemplate.opsForValue().set(key, recommendations, 10, TimeUnit.MINUTES);
        }
    }
}
```

#### 9.2.2 缓存穿透、击穿、雪崩解决方案
```java
/**
 * 防止缓存穿透：布隆过滤器
 */
@Component
public class BloomFilterService {
    
    private BloomFilter<Long> userIdBloomFilter;
    
    @PostConstruct
    public void init() {
        // 初始化布隆过滤器（预计100万用户，误判率0.01）
        userIdBloomFilter = BloomFilter.create(
            Funnels.longFunnel(),
            1000000,
            0.01
        );
        
        // 加载所有用户ID
        List<Long> userIds = userMapper.selectAllUserIds();
        userIds.forEach(userIdBloomFilter::put);
    }
    
    public boolean userExists(Long userId) {
        return userIdBloomFilter.mightContain(userId);
    }
}

/**
 * 防止缓存击穿：分布式锁
 */
@Service
public class MatchingService {
    
    @Autowired
    private RedissonClient redissonClient;
    
    public List<UserCardDTO> getRecommendUsers(Long userId) {
        String key = String.format(CacheKeys.RECOMMEND_USERS, userId, 1);
        
        // 1. 查缓存
        List<UserCardDTO> cached = (List<UserCardDTO>) redisTemplate.opsForValue().get(key);
        if (cached != null) {
            return cached;
        }
        
        // 2. 获取分布式锁
        String lockKey = "lock:recommend:" + userId;
        RLock lock = redissonClient.getLock(lockKey);
        
        try {
            // 尝试获取锁，最多等待10秒，锁30秒后自动释放
            if (lock.tryLock(10, 30, TimeUnit.SECONDS)) {
                // 3. 双重检查
                cached = (List<UserCardDTO>) redisTemplate.opsForValue().get(key);
                if (cached != null) {
                    return cached;
                }
                
                // 4. 查数据库并计算
                List<UserCardDTO> result = calculateRecommendations(userId);
                
                // 5. 写入缓存
                redisTemplate.opsForValue().set(key, result, 10, TimeUnit.MINUTES);
                
                return result;
            }
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        } finally {
            lock.unlock();
        }
        
        return Collections.emptyList();
    }
}

/**
 * 防止缓存雪崩：缓存过期时间加随机值
 */
public void setCacheWithRandomExpire(String key, Object value, long baseSeconds) {
    long randomSeconds = ThreadLocalRandom.current().nextLong(0, 300);  // 0-5分钟随机
    long expireSeconds = baseSeconds + randomSeconds;
    
    redisTemplate.opsForValue().set(key, value, expireSeconds, TimeUnit.SECONDS);
}
```

### 9.3 接口优化

#### 9.3.1 批量查询优化
```java
/**
 * 聊天列表批量查询优化
 */
@Service
public class ChatService {
    
    public List<ChatSessionDTO> getChatSessions(Long userId) {
        // 1. 查询会话列表
        List<ChatSession> sessions = chatSessionMapper.selectByUserId(userId);
        
        // 2. 提取所有用户ID
        Set<Long> userIds = sessions.stream()
            .map(session -> session.getUser1Id().equals(userId) 
                ? session.getUser2Id() 
                : session.getUser1Id())
            .collect(Collectors.toSet());
        
        // 3. 批量查询用户信息（一次查询）
        Map<Long, User> userMap = userMapper.selectBatchIds(userIds)
            .stream()
            .collect(Collectors.toMap(User::getId, u -> u));
        
        // 4. 组装DTO
        return sessions.stream()
            .map(session -> {
                Long otherUserId = session.getUser1Id().equals(userId) 
                    ? session.getUser2Id() 
                    : session.getUser1Id();
                User otherUser = userMap.get(otherUserId);
                
                ChatSessionDTO dto = new ChatSessionDTO();
                dto.setChatId(session.getId());
                dto.setNickname(otherUser.getNickname());
                dto.setAvatar(otherUser.getAvatar());
                // ... 其他字段
                return dto;
            })
        .collect(Collectors.toList());
    }
}
```

#### 9.3.2 异步处理
```java
/**
 * 异步任务配置
 */
@Configuration
@EnableAsync
public class AsyncConfig {
    
    @Bean(name = "taskExecutor")
    public Executor taskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(10);
        executor.setMaxPoolSize(20);
        executor.setQueueCapacity(200);
        executor.setThreadNamePrefix("async-");
        executor.initialize();
        return executor;
    }
}

/**
 * 异步任务示例
 */
@Service
public class NotificationService {
    
    /**
     * 异步发送通知
     */
    @Async("taskExecutor")
    public void sendNotification(Long userId, String message) {
        // 发送微信模板消息
        wxService.sendTemplateMessage(userId, message);
        
        // 记录通知日志
        notificationLogMapper.insert(new NotificationLog(userId, message));
    }
    
    /**
     * 异步内容审核
     */
    @Async("taskExecutor")
    public void auditContent(Long momentId, String content, List<String> images) {
        // 调用第三方内容审核API
        AuditResult result = contentAuditService.audit(content, images);
        
        // 更新审核状态
        if (result.isPass()) {
            momentMapper.updateAuditStatus(momentId, 1);  // 通过
        } else {
            momentMapper.updateAuditStatus(momentId, 2);  // 拒绝
        }
    }
}
```

---

## 10. 安全设计

### 10.1 接口安全

#### 10.1.1 JWT认证
```java
/**
 * JWT工具类
 */
@Component
public class JwtUtil {
    
    @Value("${jwt.secret}")
    private String secret;
    
    @Value("${jwt.expiration}")
    private Long expiration;
    
    /**
     * 生成JWT Token
     */
    public String generateToken(Long userId, String openid) {
        Map<String, Object> claims = new HashMap<>();
        claims.put("userId", userId);
        claims.put("openid", openid);
        
        return Jwts.builder()
            .setClaims(claims)
            .setSubject(String.valueOf(userId))
            .setIssuedAt(new Date())
            .setExpiration(new Date(System.currentTimeMillis() + expiration))
            .signWith(SignatureAlgorithm.HS512, secret)
            .compact();
    }
    
    /**
     * 解析JWT Token
     */
    public Claims parseToken(String token) {
        return Jwts.parser()
            .setSigningKey(secret)
            .parseClaimsJws(token)
            .getBody();
    }
    
    /**
     * 验证Token是否有效
     */
    public boolean validateToken(String token) {
        try {
            Claims claims = parseToken(token);
            return !claims.getExpiration().before(new Date());
        } catch (Exception e) {
            return false;
        }
    }
}

/**
 * JWT拦截器
 */
@Component
public class JwtInterceptor implements HandlerInterceptor {
    
    @Autowired
    private JwtUtil jwtUtil;
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                           HttpServletResponse response, 
                           Object handler) throws Exception {
        
        // 从Header中获取Token
        String token = request.getHeader("Authorization");
        if (token == null || !token.startsWith("Bearer ")) {
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            return false;
        }
        
        token = token.substring(7);
        
        // 验证Token
        if (!jwtUtil.validateToken(token)) {
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            return false;
        }
        
        // 解析Token，将用户ID放入Request
        Claims claims = jwtUtil.parseToken(token);
        Long userId = claims.get("userId", Long.class);
        request.setAttribute("userId", userId);
        
        return true;
    }
}
```

#### 10.1.2 接口限流
```java
/**
 * 限流注解
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RateLimit {
    int limit() default 10;  // 限制次数
    int period() default 60;  // 时间窗口（秒）
}

/**
 * 限流AOP
 */
@Aspect
@Component
public class RateLimitAspect {
    
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    
    @Around("@annotation(rateLimit)")
    public Object around(ProceedingJoinPoint joinPoint, RateLimit rateLimit) throws Throwable {
        // 获取用户ID
        ServletRequestAttributes attributes = 
            (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
        HttpServletRequest request = attributes.getRequest();
        Long userId = (Long) request.getAttribute("userId");
        
        // 限流Key
        String key = String.format("rate:limit:%s:%s", 
            joinPoint.getSignature().getName(), 
            userId);
        
        // 获取当前计数
        Integer count = (Integer) redisTemplate.opsForValue().get(key);
        
        if (count == null) {
            // 第一次访问
            redisTemplate.opsForValue().set(key, 1, rateLimit.period(), TimeUnit.SECONDS);
        } else if (count < rateLimit.limit()) {
            // 未超限，计数+1
            redisTemplate.opsForValue().increment(key);
    } else {
            // 超限，拒绝请求
            throw new BusinessException("请求过于频繁，请稍后再试");
        }
        
        return joinPoint.proceed();
    }
}

/**
 * 使用示例
 */
@RestController
@RequestMapping("/api/moments")
public class MomentController {
    
    @PostMapping("/publish")
    @RateLimit(limit = 3, period = 86400)  // 每天最多发布3条
    public Result<MomentDTO> publishMoment(@RequestBody MomentRequest request) {
        // ...
    }
    
    @PostMapping("/like")
    @RateLimit(limit = 100, period = 60)  // 每分钟最多点赞100次
    public Result<Void> likeMoment(@RequestBody LikeRequest request) {
        // ...
    }
}
```

### 10.2 数据安全

#### 10.2.1 敏感信息加密
```java
/**
 * 数据脱敏工具类
 */
public class DesensitizationUtil {
    
    /**
     * 手机号脱敏：138****8888
     */
    public static String desensitizePhone(String phone) {
        if (StringUtils.isEmpty(phone) || phone.length() != 11) {
            return phone;
        }
        return phone.substring(0, 3) + "****" + phone.substring(7);
    }
    
    /**
     * 身份证号脱敏：110101********1234
     */
    public static String desensitizeIdCard(String idCard) {
        if (StringUtils.isEmpty(idCard) || idCard.length() != 18) {
            return idCard;
        }
        return idCard.substring(0, 6) + "********" + idCard.substring(14);
    }
    
    /**
     * 真实姓名脱敏：张*
     */
    public static String desensitizeName(String name) {
        if (StringUtils.isEmpty(name) || name.length() < 2) {
            return name;
        }
        return name.substring(0, 1) + "*".repeat(name.length() - 1);
    }
}

/**
 * 敏感信息加密存储
 */
@Component
public class EncryptionService {
    
    @Value("${encryption.secret}")
    private String secret;
    
    /**
     * AES加密
     */
    public String encrypt(String plainText) {
        try {
            SecretKeySpec key = new SecretKeySpec(secret.getBytes(), "AES");
            Cipher cipher = Cipher.getInstance("AES/ECB/PKCS5Padding");
            cipher.init(Cipher.ENCRYPT_MODE, key);
            byte[] encrypted = cipher.doFinal(plainText.getBytes());
            return Base64.getEncoder().encodeToString(encrypted);
        } catch (Exception e) {
            throw new BusinessException("加密失败");
        }
    }
    
    /**
     * AES解密
     */
    public String decrypt(String encryptedText) {
        try {
            SecretKeySpec key = new SecretKeySpec(secret.getBytes(), "AES");
            Cipher cipher = Cipher.getInstance("AES/ECB/PKCS5Padding");
            cipher.init(Cipher.DECRYPT_MODE, key);
            byte[] decrypted = cipher.doFinal(Base64.getDecoder().decode(encryptedText));
            return new String(decrypted);
        } catch (Exception e) {
            throw new BusinessException("解密失败");
        }
    }
}
```

#### 10.2.2 SQL注入防护
```java
/**
 * 使用MyBatis-Plus防止SQL注入
 */
@Service
public class UserService {
    
    @Autowired
    private UserMapper userMapper;
    
    /**
     * 正确的做法：使用参数化查询
     */
    public List<User> searchUsers(String keyword) {
        LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
        wrapper.like(User::getNickname, keyword)
               .or()
               .like(User::getBio, keyword);
        
        return userMapper.selectList(wrapper);
    }
    
    /**
     * 错误的做法：拼接SQL（永远不要这样做）
     */
    // public List<User> searchUsersUnsafe(String keyword) {
    //     String sql = "SELECT * FROM t_user WHERE nickname LIKE '%" + keyword + "%'";
    //     return userMapper.selectBySql(sql);  // 危险！
    // }
}
```

### 10.3 内容安全

#### 10.3.1 内容审核
```java
/**
 * 内容审核服务
 */
@Service
public class ContentAuditService {
    
    /**
     * 敏感词过滤
     */
    public boolean containsSensitiveWords(String content) {
        // 加载敏感词库
        Set<String> sensitiveWords = loadSensitiveWords();
        
        // 使用DFA算法检测敏感词
        for (String word : sensitiveWords) {
            if (content.contains(word)) {
                return true;
            }
        }
        
        return false;
    }
    
    /**
     * 调用第三方审核API
     */
    public AuditResult auditContent(String content, List<String> images) {
        // 调用腾讯云内容安全API
        JSONObject result = tencentCloudService.textModeration(content);
        
        if (result.getString("Suggestion").equals("Block")) {
            return AuditResult.rejected("包含违规内容");
        }
        
        // 图片审核
        for (String imageUrl : images) {
            JSONObject imageResult = tencentCloudService.imageModeration(imageUrl);
            if (imageResult.getString("Suggestion").equals("Block")) {
                return AuditResult.rejected("图片包含违规内容");
            }
        }
        
        return AuditResult.passed();
    }
}
```

---

## 11. 部署方案

### 11.1 开发环境
```yaml
# application-dev.yml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pengpeng_dev
    username: root
    password: root
  
  redis:
    host: localhost
    port: 6379

# 微信小程序配置（测试账号）
wx:
  appid: wxtest123
  secret: test_secret

# 会员功能开关（开发环境默认关闭）
vip:
  feature:
    enabled: false
```

### 11.2 生产环境
```yaml
# application-prod.yml
server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://rm-xxx.mysql.rds.aliyuncs.com:3306/pengpeng_prod
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  
  redis:
    cluster:
      nodes:
        - redis-1:6379
        - redis-2:6379
        - redis-3:6379

# 微信小程序配置（正式账号）
wx:
  appid: ${WX_APPID}
  secret: ${WX_SECRET}

# 会员功能开关（生产环境从数据库读取）
vip:
  feature:
    enabled: ${VIP_FEATURE_ENABLED:false}
```

### 11.3 Docker部署
```dockerfile
# Dockerfile
FROM openjdk:8-jre-alpine

WORKDIR /app

COPY target/pengpeng-0.0.1-SNAPSHOT.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "-Dspring.profiles.active=prod", "app.jar"]
```

```yaml
# docker-compose.yml
version: '3'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_USERNAME=root
      - DB_PASSWORD=password
      - WX_APPID=wxc5c00133500315e3
      - WX_SECRET=908f9750e78217b8083bda7c27ea58b2
    depends_on:
      - mysql
      - redis
  
  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=pengpeng_prod
    volumes:
      - mysql_data:/var/lib/mysql
  
  redis:
    image: redis:6-alpine
    ports:
      - "6379:6379"

volumes:
  mysql_data:
```

---

## 12. 监控与运维

### 12.1 日志管理
```xml
<!-- logback-spring.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 日志输出格式 -->
    <property name="LOG_PATTERN" 
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"/>
    
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    
    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/pengpeng.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/pengpeng.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    
    <!-- 错误日志单独输出 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/pengpeng-error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/pengpeng-error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
```

### 12.2 监控指标
```java
/**
 * 业务监控指标
 */
@Component
public class MetricsService {
    
    @Autowired
    private MeterRegistry meterRegistry;
    
    /**
     * 记录匹配次数
     */
    public void recordMatchCount(String matchType) {
        Counter counter = meterRegistry.counter("match.count", "type", matchType);
        counter.increment();
    }
    
    /**
     * 记录匹配成功率
     */
    public void recordMatchSuccess() {
        Counter counter = meterRegistry.counter("match.success");
        counter.increment();
    }
    
    /**
     * 记录接口响应时间
     */
    public void recordApiDuration(String api, long duration) {
        Timer timer = meterRegistry.timer("api.duration", "api", api);
        timer.record(duration, TimeUnit.MILLISECONDS);
    }
}
```

---

## 13. 附录

### 13.1 错误码定义
```java
/**
 * 业务错误码
 */
public enum ErrorCode {
    
    // 通用错误 (1000-1999)
    SUCCESS(0, "成功"),
    SYSTEM_ERROR(1000, "系统错误"),
    PARAM_ERROR(1001, "参数错误"),
    UNAUTHORIZED(1002, "未授权"),
    FORBIDDEN(1003, "无权限"),
    NOT_FOUND(1004, "资源不存在"),
    
    // 用户相关 (2000-2999)
    USER_NOT_FOUND(2000, "用户不存在"),
    USER_ALREADY_EXISTS(2001, "用户已存在"),
    PROFILE_INCOMPLETE(2002, "资料未完善"),
    NICKNAME_DUPLICATE(2003, "昵称已被使用"),
    
    // 匹配相关 (3000-3999)
    MATCH_LIMIT_EXCEEDED(3000, "今日匹配次数已用完"),
    ALREADY_MATCHED(3001, "已经匹配过了"),
    CANNOT_MATCH_SELF(3002, "不能匹配自己"),
    
    // 聊天相关 (4000-4999)
    CHAT_NOT_FOUND(4000, "聊天不存在"),
    MESSAGE_LIMIT_EXCEEDED(4001, "对方未回复，无法继续发送"),
    BLOCKED_BY_USER(4002, "已被对方拉黑"),
    
    // 动态相关 (5000-5999)
    MOMENT_PUBLISH_LIMIT(5000, "今日发布次数已用完"),
    MOMENT_UNDER_REVIEW(5001, "动态审核中"),
    MOMENT_REJECTED(5002, "动态审核未通过"),
    SENSITIVE_CONTENT(5003, "内容包含敏感信息"),
    
    // 会员相关 (6000-6999)
    NOT_VIP_MEMBER(6000, "非会员用户"),
    VIP_EXPIRED(6001, "会员已过期"),
    PAYMENT_FAILED(6002, "支付失败"),
    ORDER_NOT_FOUND(6003, "订单不存在"),
    
    // 活动相关 (7000-7999)
    ACTIVITY_FULL(7000, "活动已满员"),
    ALREADY_REGISTERED(7001, "已经报名过了"),
    ACTIVITY_EXPIRED(7002, "活动已过期"),
    REGISTRATION_LIMIT(7003, "发布活动次数已达上限");
    
    private final int code;
    private final String message;
    
    ErrorCode(int code, String message) {
        this.code = code;
        this.message = message;
    }
    
    // getters...
}
```

### 13.2 开发规范
```java
/**
 * Controller层规范
 */
@RestController
@RequestMapping("/api/users")
@Api(tags = "用户管理")
@Slf4j
public class UserController {
    
    /**
     * 1. 使用@ApiOperation注解说明接口用途
     * 2. 使用@Validated进行参数校验
     * 3. 统一使用Result包装返回值
     * 4. 记录关键操作日志
     * 5. 捕获并处理异常
     */
    @PostMapping("/profile")
    @ApiOperation("更新用户资料")
    public Result<UserProfileDTO> updateProfile(
            @Validated @RequestBody UserProfileRequest request,
            HttpServletRequest httpRequest) {
        
        Long userId = (Long) httpRequest.getAttribute("userId");
        log.info("用户{}更新资料: {}", userId, request);
        
        try {
            UserProfileDTO profile = userService.updateProfile(userId, request);
            return Result.success(profile);
        } catch (BusinessException e) {
            log.error("更新资料失败", e);
            return Result.error(e.getCode(), e.getMessage());
        }
    }
}

/**
 * Service层规范
 */
@Service
@Slf4j
public class UserServiceImpl implements UserService {
    
    /**
     * 1. 使用@Transactional管理事务
     * 2. 参数校验
     * 3. 业务逻辑清晰
     * 4. 异常统一抛出BusinessException
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public UserProfileDTO updateProfile(Long userId, UserProfileRequest request) {
        // 1. 参数校验
        if (userId == null) {
            throw new BusinessException(ErrorCode.PARAM_ERROR);
        }
        
        // 2. 查询用户
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException(ErrorCode.USER_NOT_FOUND);
        }
        
        // 3. 更新资料
        UserProfile profile = new UserProfile();
        BeanUtils.copyProperties(request, profile);
        profile.setUserId(userId);
        userProfileMapper.updateById(profile);
        
        // 4. 删除缓存
        cacheService.deleteUserCache(userId);
        
        // 5. 返回结果
        return convertToDTO(profile);
    }
}
```

---

## 14. 总结

本系统分析文档详细描述了"碰碰"交友社交小程序的完整技术实现方案，包括：

### 14.1 核心特性
1. **双模式运营** - 恋爱交友 + 搭子平台
2. **智能匹配算法** - 多维度权重计算
3. **会员功能开关** - 前期免费，后期付费
4. **防骚扰机制** - 保护用户体验
5. **内容审核机制** - 保证平台安全

### 14.2 技术亮点
1. **第三方IM集成** - 使用腾讯云IM，简化聊天系统，提升稳定性
2. **缓存策略** - Redis多级缓存，提升性能
3. **异步处理** - 消息通知、内容审核异步化
4. **分布式锁** - 防止缓存击穿
5. **接口限流** - 防止恶意请求
6. **数据脱敏** - 保护用户隐私

### 14.3 下一步工作
1. **代码实现** - 按照本文档进行开发
2. **单元测试** - 保证代码质量
3. **压力测试** - 验证系统性能
4. **灰度发布** - 逐步上线新功能
5. **数据分析** - 监控关键指标，优化产品

---

**文档版本**: v1.1（聊天模块改用第三方IM）  
**编写日期**: 2025-01-01  
**更新日期**: 2025-11-01  
**适用版本**: wxmini-friends 1.0+  
**作者**: AI Assistant (Claude Sonnet 4.5)

---

📌 **注意事项**：
- 本文档已移除学历认证相关功能
- 会员功能默认关闭，通过配置开关控制
- **聊天功能使用腾讯云IM**，需要在腾讯云控制台申请SDKAPPID和密钥
- 前期免费额度足够使用（100万DAU以下）
- 所有接口需要JWT认证（除登录接口）
- 敏感信息需加密存储
- 定期备份数据库
- 及时更新依赖版本，修复安全漏洞

💡 **开发建议**：
- 优先实现MVP核心功能（匹配、动态、搭子）
- 聊天功能集成腾讯云IM SDK（2-3天完成）
- 配置IM回调地址用于防骚扰机制
- 使用Git进行版本控制
- 代码review保证质量
- 编写详细的接口文档（Swagger）
- 做好异常处理和日志记录
- 前后端联调时使用Mock数据

🎯 **聊天模块实施步骤**：
1. 在腾讯云控制台创建IM应用，获取SDKAPPID和密钥
2. 后端集成SDK，实现UserSig生成和权限检查接口
3. 小程序端集成tim-wx-sdk
4. 配置IM回调地址，实现防骚扰计数
5. 测试单聊、消息推送、离线消息等功能

🎯 **成功标准**：
- 匹配成功率 > 30%
- 日活跃用户 > 1000人
- 用户留存率（7日） > 40%
- 平均响应时间 < 500ms
- 系统可用性 > 99.9%

---

---

## 15. 技术实现细节补充

### 15.1 动态广场模块技术实现

#### 15.1.1 统一表结构设计思想

**设计思想**：采用单表设计，通过 `momentType` 字段区分不同类型的动态/活动，避免表碎片化。

```
t_moment (统一表)
├── momentType = 1 → 搭子动态
├── momentType = 2 → 恋爱动态
├── momentType = 3 → 搭子活动 (用户发起)
└── momentType = 4 → 官方活动 (平台发起)
```

**设计优势**：
- ✅ 统一查询和展示逻辑
- ✅ 减少表数量，简化维护
- ✅ 灵活扩展新类型
- ✅ 统一的点赞、评论、统计机制

#### 15.1.2 活动状态自动管理

```java
/**
 * 定时任务：每小时检查过期活动
 */
@Scheduled(cron = "0 0 * * * ?")
public void updateExpiredActivities() {
    LambdaUpdateWrapper<Moment> updateWrapper = new LambdaUpdateWrapper<>();
    updateWrapper
        .in(Moment::getMomentType, 3, 4) // 活动类型
        .lt(Moment::getActivityTime, LocalDateTime.now()) // 时间已过
        .eq(Moment::getActivityStatus, 1) // 当前是招募中
        .set(Moment::getActivityStatus, 3); // 改为已结束
    
    momentMapper.update(null, updateWrapper);
}
```

#### 15.1.3 查询性能优化

**索引设计**
```sql
-- 按用户查询
INDEX idx_user_id (user_id)

-- 按类型筛选
INDEX idx_moment_type (moment_type)

-- 城市+类型组合查询
INDEX idx_city_type (city, moment_type)

-- 活动时间范围查询
INDEX idx_activity_time (activity_time)
```

**统计字段冗余**
```java
// ✅ 冗余字段（性能好）
SELECT like_count FROM t_moment WHERE id = ?

// ❌ 实时统计（性能差）
SELECT COUNT(*) FROM t_moment_like WHERE moment_id = ?
```

---

### 15.2 微信支付技术实现

#### 15.2.1 支付签名生成

```java
/**
 * 生成微信支付签名（MD5）
 */
private String generateSign(Map<String, Object> params) {
    // 1. 参数排序
    List<String> keys = new ArrayList<>(params.keySet());
    Collections.sort(keys);
    
    // 2. 拼接字符串
    StringBuilder sb = new StringBuilder();
    for (String key : keys) {
        Object value = params.get(key);
        if (value != null && !"".equals(value) && !"sign".equals(key)) {
            sb.append(key).append("=").append(value).append("&");
        }
    }
    sb.append("key=").append(wxPayConfig.getApiKey());
    
    // 3. MD5加密并转大写
    return DigestUtil.md5Hex(sb.toString()).toUpperCase();
}
```

#### 15.2.2 支付回调处理（幂等性）

```java
@Override
@Transactional(rollbackFor = Exception.class)
public String handleWxPayNotify(String requestBody, String signature, 
                                 String timestamp, String nonce) {
    // 1. 解析XML数据
    Map<String, String> result = xmlToMap(requestBody);
    
    // 2. 验证返回结果
    if (!"SUCCESS".equals(result.get("return_code"))) {
        return buildNotifyResponse(false, result.get("return_msg"));
    }
    
    // 3. 获取订单信息
    String orderNo = result.get("out_trade_no");
    Order order = orderMapper.selectOne(
        new LambdaQueryWrapper<Order>()
            .eq(Order::getOrderNo, orderNo)
    );
    
    // 4. 验证金额
    int totalFee = Integer.parseInt(result.get("total_fee"));
    int orderAmount = order.getAmount().multiply(new BigDecimal("100")).intValue();
    if (totalFee != orderAmount) {
        return buildNotifyResponse(false, "订单金额不匹配");
    }
    
    // 5. 检查订单状态（幂等性）⭐
    if (order.getPaymentStatus() == 1) {
        return buildNotifyResponse(true, "订单已处理");
    }
    
    // 6. 更新订单状态
    orderMapper.update(null,
        new LambdaUpdateWrapper<Order>()
            .eq(Order::getId, order.getId())
            .set(Order::getPaymentStatus, 1)
            .set(Order::getTransactionId, result.get("transaction_id"))
            .set(Order::getPaidAt, LocalDateTime.now())
    );
    
    // 7. 开通VIP
    activateVip(order);
    
    return buildNotifyResponse(true, "SUCCESS");
}
```

#### 15.2.3 VIP自动开通逻辑

```java
private void activateVip(Order order) {
    String userId = order.getUserId();
    Integer vipLevel = order.getVipLevel();
    Integer days = VIP_DAYS.get(vipLevel); // 30/90/365天
    
    VipMember existingMember = vipMemberMapper.selectOne(
        new LambdaQueryWrapper<VipMember>()
            .eq(VipMember::getUserId, userId)
    );
    
    LocalDateTime now = LocalDateTime.now();
    LocalDateTime startTime;
    LocalDateTime expireTime;
    
    if (existingMember != null && existingMember.getExpireTime().isAfter(now)) {
        // 有效期内续费，延长时间 ⭐
        startTime = existingMember.getStartTime();
        expireTime = existingMember.getExpireTime().plusDays(days);
        
        vipMemberMapper.update(null,
            new LambdaUpdateWrapper<VipMember>()
                .eq(VipMember::getId, existingMember.getId())
                .set(VipMember::getVipLevel, vipLevel)
                .set(VipMember::getExpireTime, expireTime)
        );
    } else {
        // 新开通或过期后重新开通
        startTime = now;
        expireTime = now.plusDays(days);
        
        if (existingMember != null) {
            vipMemberMapper.update(null,
                new LambdaUpdateWrapper<VipMember>()
                    .eq(VipMember::getId, existingMember.getId())
                    .set(VipMember::getVipLevel, vipLevel)
                    .set(VipMember::getStartTime, startTime)
                    .set(VipMember::getExpireTime, expireTime)
            );
        } else {
            VipMember newMember = new VipMember();
            newMember.setUserId(userId);
            newMember.setVipLevel(vipLevel);
            newMember.setStartTime(startTime);
            newMember.setExpireTime(expireTime);
            newMember.setAutoRenew(0);
            vipMemberMapper.insert(newMember);
        }
    }
}
```

#### 15.2.4 配置说明

**application-payment.yml**
```yaml
wx:
  pay:
    # 商户号
    mch-id: 1234567890
    
    # API密钥（32位）
    api-key: your_32_character_api_key_here
    
    # API V3密钥
    api-v3-key: your_32_character_v3_key_here
    
    # 证书序列号
    cert-serial-no: 5B3D2F9A1C7E8D4B2A6F1E9C8D7B5A4E
    
    # 证书路径
    private-key-path: classpath:cert/apiclient_key.pem
    cert-path: classpath:cert/apiclient_cert.pem
    
    # 回调地址（必须HTTPS）
    notify-url: https://your-domain.com/api/payment/wx-notify
    
    # API地址
    api-url: https://api.mch.weixin.qq.com
```

**证书文件结构**
```
src/main/resources/cert/
├── apiclient_cert.pem  # 商户证书
└── apiclient_key.pem   # 商户私钥
```

#### 15.2.5 安全措施

1. **签名验证**：所有请求都需要MD5签名
2. **金额校验**：回调时验证订单金额
3. **幂等性处理**：防止重复支付
4. **HTTPS传输**：回调地址必须是HTTPS
5. **敏感信息加密**：API密钥不提交到Git

```java
// 日志脱敏
log.info("订单创建成功，订单号：{}，金额：{}", orderNo, amount);
// ❌ 不要打印完整的API密钥

// 签名验证
boolean isValid = verifySignature(requestBody, signature, timestamp, nonce);
if (!isValid) {
    throw new SecurityException("签名验证失败");
}
```

#### 15.2.6 前端调用示例

```javascript
// 1. 创建订单
async function createOrder(vipLevel) {
  const res = await wx.request({
    url: 'https://api.example.com/api/payment/create-order',
    method: 'POST',
    header: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    data: { vipLevel: vipLevel }
  });
  
  return res.data.data;
}

// 2. 调起支付
async function pay(orderData) {
  const payParams = orderData.wxPayParams;
  
  try {
    await wx.requestPayment({
      timeStamp: payParams.timeStamp,
      nonceStr: payParams.nonceStr,
      package: payParams.packageValue,
      signType: payParams.signType,
      paySign: payParams.paySign
    });
    
    // 支付成功
    console.log('支付成功');
    
    // 查询订单状态
    await checkOrderStatus(orderData.orderNo);
    
  } catch (err) {
    console.error('支付失败', err);
  }
}

// 3. 查询订单状态
async function checkOrderStatus(orderNo) {
  const res = await wx.request({
    url: `https://api.example.com/api/payment/order/${orderNo}`,
    method: 'GET',
    header: { 'Authorization': `Bearer ${token}` }
  });
  
  if (res.data.data.paymentStatus === 1) {
    console.log('支付已完成，VIP已开通');
  }
}
```

#### 15.2.7 测试与调试

**使用内网穿透（开发调试）**
```bash
# 使用ngrok
ngrok http 8080

# 获得公网地址
https://abc123.ngrok.io

# 配置回调地址
notify-url: https://abc123.ngrok.io/api/payment/wx-notify
```

**测试用例**
```bash
# 1. 创建订单
curl -X POST http://localhost:8080/api/payment/create-order \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"vipLevel": 1}'

# 2. 查询订单
curl -X GET http://localhost:8080/api/payment/order/PO1699876543210ABC \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. 取消订单
curl -X POST http://localhost:8080/api/payment/order/PO1699876543210ABC/cancel \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

**文档版本**: v2.0（完整业务逻辑流程图 + 技术实现细节）  
**最后更新**: 2025-11-07  
**适用版本**: wxmini-friends 2.0+  

**文档结束**